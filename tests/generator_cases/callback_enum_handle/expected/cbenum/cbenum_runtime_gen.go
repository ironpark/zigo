// Code generated by zigo. DO NOT EDIT.

package cbenum

import (
	"runtime/cgo"
	"sync/atomic"
	"unsafe"

	"example.com/zigo/cbenum/internal/raw"
)

// zigoHandle is what every handle and borrowed reference offers a generated
// call: pin it open for the native call, let it go afterwards, and mark it
// unusable when the call ended in a Zig panic.
type zigoHandle interface {
	zigoAcquire(operation string) (unsafe.Pointer, error)
	zigoRelease()
	zigoPoison(cause *NativePanicError)
}

// zigoCheckedPointer pins value open for the rest of the call and hands back
// its native pointer; the caller defers zigoRelease. A nil, closed, or
// poisoned handle is the error instead, and nothing is pinned.
func zigoCheckedPointer(operation string, value zigoHandle) (unsafe.Pointer, error) {
	return value.zigoAcquire(operation)
}

// zigoPoisonAfterPanic marks every handle a call reached unusable when that
// call ended in a Zig panic: the panic unwound the native frames without
// running their defers, so what is behind those handles is unknown. Any
// other error passes through untouched.
func zigoPoisonAfterPanic(err error, handles ...zigoHandle) error {
	if cause, ok := err.(*NativePanicError); ok {
		for _, handle := range handles {
			handle.zigoPoison(cause)
		}
	}
	return err
}

// OnLocationCallback is the Go callback signature accepted by the generated binding.
type OnLocationCallback func(Location)

// OnStreamCallback is the Go callback signature accepted by the generated binding.
type OnStreamCallback func(*Stream, Location)

// OnViewCallback is the Go callback signature accepted by the generated binding.
type OnViewCallback func(*Stream) bool

// PickLocationCallback is the Go callback signature accepted by the generated binding.
type PickLocationCallback func(int32) Location

func zigoBoolToUint8(value bool) uint8 {
	if value {
		return 1
	}
	return 0
}

var zigoActiveCallbackHandles atomic.Int64

type zigoCallbackHandle = cgo.Handle

func zigoNewOnLocationCallbackHandle(value OnLocationCallback) zigoCallbackHandle {
	stored := func(p0 int32) {
		value(Location(p0))
	}
	handle := cgo.NewHandle(&raw.CallbackState{Fn: stored})
	zigoActiveCallbackHandles.Add(1)
	return handle
}

func zigoNewOnStreamCallbackHandle(value OnStreamCallback) zigoCallbackHandle {
	stored := func(p0 unsafe.Pointer, p1 int32) {
		value(p0, Location(p1))
	}
	handle := cgo.NewHandle(&raw.CallbackState{Fn: stored})
	zigoActiveCallbackHandles.Add(1)
	return handle
}

func zigoNewOnViewCallbackHandle(value OnViewCallback) zigoCallbackHandle {
	stored := func(p0 unsafe.Pointer) uint8 {
		return zigoBoolToUint8(value(p0))
	}
	handle := cgo.NewHandle(&raw.CallbackState{Fn: stored})
	zigoActiveCallbackHandles.Add(1)
	return handle
}

func zigoNewPickLocationCallbackHandle(value PickLocationCallback) zigoCallbackHandle {
	stored := func(p0 int32) int32 {
		return int32(value(p0))
	}
	handle := cgo.NewHandle(&raw.CallbackState{Fn: stored})
	zigoActiveCallbackHandles.Add(1)
	return handle
}

func zigoDeleteCallbackHandle(handle zigoCallbackHandle) {
	if handle == 0 { return }
	handle.Delete()
	zigoActiveCallbackHandles.Add(-1)
}

// zigoCallbackPanicPending is the fast-path check ahead of the per-slot sweep:
// one atomic load says whether any callback recorded a panic nobody has taken.
func zigoCallbackPanicPending() bool { return raw.PendingCallbackPanics() != 0 }

// zigoRethrowCallbackPanic resumes a panic that a Go callback raised inside
// the native call that has just returned. The trampoline recovered it so the
// native frames could unwind; the caller sees it as a *CallbackPanicError.
func zigoRethrowCallbackPanic(operation string, handle zigoCallbackHandle) {
	if handle == 0 { return }
	if value, stack, ok := raw.TakeCallbackPanic(handle); ok {
		panic(&CallbackPanicError{Operation: operation, Value: value, Stack: stack})
	}
}
