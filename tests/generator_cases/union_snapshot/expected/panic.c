// Code generated by zigo. DO NOT EDIT.
#include <setjmp.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "zigo_signal.h"

// ELF and Mach-O export every non-static symbol of a shared library;
// COFF exports nothing without an explicit annotation, so a DLL built
// without this would load and then resolve none of its entry points.
#ifndef ZIGO_EXPORT
#if defined(_WIN32)
#define ZIGO_EXPORT __declspec(dllexport)
#else
#define ZIGO_EXPORT
#endif
#endif

static _Thread_local jmp_buf zg_panic_env;
static _Thread_local int zg_panic_active;
static _Thread_local char zg_panic_message[1024];

// A panic with nowhere to be reported is what Zig says it is: fatal.
// The message is written out first so the process does not die silent.
_Noreturn static void zg_panic_fatal(void) {
    fputs("zigo: native panic: ", stderr);
    fputs(zg_panic_message, stderr);
    fputc('\n', stderr);
    fflush(stderr);
    abort();
}

void zg_panic_bridge(const uint8_t *message, size_t length) {
    size_t count = length < sizeof(zg_panic_message) - 1 ? length : sizeof(zg_panic_message) - 1;
    memcpy(zg_panic_message, message, count);
    zg_panic_message[count] = '\0';
    if (zg_panic_active) longjmp(zg_panic_env, 1);
    zg_panic_fatal();
}

ZIGO_EXPORT const char *zg_last_error_message(void) { return zg_panic_message; }

int32_t zg_signal_create_impl(zg_signal * * out_result);
ZIGO_EXPORT int32_t zg_signal_create(zg_signal * * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_signal_create_impl(out_result);
    zg_panic_active = 0;
    return result;
}

int32_t zg_signal_deinit_impl(zg_signal * self);
ZIGO_EXPORT int32_t zg_signal_deinit(zg_signal * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_signal_deinit_impl(self);
    zg_panic_active = 0;
    return result;
}

int32_t zg_signal_set_ticks_impl(zg_signal * self, uint32_t ticks);
ZIGO_EXPORT int32_t zg_signal_set_ticks(zg_signal * self, uint32_t ticks) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_signal_set_ticks_impl(self, ticks);
    zg_panic_active = 0;
    return result;
}

int32_t zg_signal_set_mode_impl(zg_signal * self, uint8_t mode);
ZIGO_EXPORT int32_t zg_signal_set_mode(zg_signal * self, uint8_t mode) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_signal_set_mode_impl(self, mode);
    zg_panic_active = 0;
    return result;
}

int32_t zg_signal_set_active_impl(zg_signal * self, uint8_t active);
ZIGO_EXPORT int32_t zg_signal_set_active(zg_signal * self, uint8_t active) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_signal_set_active_impl(self, active);
    zg_panic_active = 0;
    return result;
}

uint8_t zg_signal_project_tag_impl(const zg_signal *self, uint8_t *out_value);
ZIGO_EXPORT uint8_t zg_signal_project_tag(const zg_signal *self, uint8_t *out_value) {
    if (self == NULL || out_value == NULL) return 2;
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 3;
    }
    uint8_t result = zg_signal_project_tag_impl(self, out_value);
    zg_panic_active = 0;
    return result;
}

uint8_t zg_signal_project_ticks_impl(const zg_signal *self, uint32_t *out_value);
ZIGO_EXPORT uint8_t zg_signal_project_ticks(const zg_signal *self, uint32_t *out_value) {
    if (self == NULL || out_value == NULL) return 2;
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 3;
    }
    uint8_t result = zg_signal_project_ticks_impl(self, out_value);
    zg_panic_active = 0;
    return result;
}

uint8_t zg_signal_project_level_impl(const zg_signal *self, double *out_value);
ZIGO_EXPORT uint8_t zg_signal_project_level(const zg_signal *self, double *out_value) {
    if (self == NULL || out_value == NULL) return 2;
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 3;
    }
    uint8_t result = zg_signal_project_level_impl(self, out_value);
    zg_panic_active = 0;
    return result;
}

uint8_t zg_signal_project_offset_impl(const zg_signal *self, int16_t *out_value);
ZIGO_EXPORT uint8_t zg_signal_project_offset(const zg_signal *self, int16_t *out_value) {
    if (self == NULL || out_value == NULL) return 2;
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 3;
    }
    uint8_t result = zg_signal_project_offset_impl(self, out_value);
    zg_panic_active = 0;
    return result;
}

uint8_t zg_signal_project_mode_impl(const zg_signal *self, uint8_t *out_value);
ZIGO_EXPORT uint8_t zg_signal_project_mode(const zg_signal *self, uint8_t *out_value) {
    if (self == NULL || out_value == NULL) return 2;
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 3;
    }
    uint8_t result = zg_signal_project_mode_impl(self, out_value);
    zg_panic_active = 0;
    return result;
}

uint8_t zg_signal_project_active_impl(const zg_signal *self, uint8_t *out_value);
ZIGO_EXPORT uint8_t zg_signal_project_active(const zg_signal *self, uint8_t *out_value) {
    if (self == NULL || out_value == NULL) return 2;
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 3;
    }
    uint8_t result = zg_signal_project_active_impl(self, out_value);
    zg_panic_active = 0;
    return result;
}

uint8_t zg_signal_snapshot_impl(const zg_signal *self, zg_signal_snapshot_t *out_snapshot);
ZIGO_EXPORT uint8_t zg_signal_snapshot(const zg_signal *self, zg_signal_snapshot_t *out_snapshot) {
    if (self == NULL || out_snapshot == NULL) return 2;
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 3;
    }
    uint8_t result = zg_signal_snapshot_impl(self, out_snapshot);
    zg_panic_active = 0;
    return result;
}
