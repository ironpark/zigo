// Code generated by zigo. DO NOT EDIT.
const std = @import("std");
const target = @import("zigo_target");

extern fn zg_panic_bridge(message: [*]const u8, length: usize) callconv(.c) noreturn;
fn panicHandler(message: []const u8, _: ?usize) noreturn {
    zg_panic_bridge(message.ptr, message.len);
}
pub const panic = std.debug.FullPanic(panicHandler);

export fn zg_measure_impl(text_ptr: [*c]const u8, text_len: usize) usize {
    return target.measure(if (text_ptr) |zigo_text| zigo_text[0..text_len] else null);
}
export fn zg_label_impl(text: ?[*:0]const u8) usize {
    return target.label(text);
}
export fn zg_total_impl(values_ptr: [*c]const i32, values_len: usize) i32 {
    return target.total(if (values_ptr) |zigo_values| zigo_values[0..values_len] else null);
}
export fn zg_digits_impl(count: u32, out_result_ptr: *[*c]const i32, out_result_len: *usize) void {
    const result = target.digits(count) orelse {
        out_result_ptr.* = null;
        out_result_len.* = 0;
        return;
    };
    out_result_ptr.* = result.ptr;
    out_result_len.* = result.len;
}
export fn zg_name_impl(count: u32, out_result_ptr: *[*c]const u8, out_result_len: *usize) void {
    const result = target.name(count) orelse {
        out_result_ptr.* = null;
        out_result_len.* = 0;
        return;
    };
    out_result_ptr.* = result.ptr;
    out_result_len.* = result.len;
}
export fn zg_checked_digits_impl(count: u32, out_result_ptr: *[*c]const i32, out_result_len: *usize) i32 {
    const result = target.checkedDigits(count) catch |err| return switch (err) {
        error.Invalid => 1,
    };
    if (result) |zigo_value| {
        out_result_ptr.* = zigo_value.ptr;
        out_result_len.* = zigo_value.len;
    } else {
        out_result_ptr.* = null;
        out_result_len.* = 0;
    }
    return 0;
}
