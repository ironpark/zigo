// Code generated by zigo. DO NOT EDIT.

package input

import "strconv"

// EnumParseError reports text that names no value of a generated enum.
type EnumParseError struct {
	// Type is the Go enum type name.
	Type string
	// Text is the rejected input.
	Text string
}

// Error implements error.
func (err *EnumParseError) Error() string {
	return "zigo: " + err.Type + ": unknown value " + strconv.Quote(err.Text)
}

// Key represents the corresponding Zig enum.
type Key uint8

const (
	// KeyA corresponds to the Zig tag a.
	KeyA Key = 0
	// KeyEnter corresponds to the Zig tag enter.
	KeyEnter Key = 1
)

// String returns the Zig tag name.
func (value Key) String() string {
	switch value {
	case KeyA:
		return "a"
	case KeyEnter:
		return "enter"
	default:
		return "Key(" + strconv.Itoa(int(value)) + ")"
	}
}

// ParseKey returns the Key named by text, which is a Zig tag name.
func ParseKey(text string) (Key, error) {
	switch text {
	case "a":
		return KeyA, nil
	case "enter":
		return KeyEnter, nil
	}
	return 0, &EnumParseError{Type: "Key", Text: text}
}

// MarshalText implements encoding.TextMarshaler with the String spelling.
func (value Key) MarshalText() ([]byte, error) {
	return []byte(value.String()), nil
}

// UnmarshalText implements encoding.TextUnmarshaler with ParseKey.
func (value *Key) UnmarshalText(text []byte) error {
	parsed, err := ParseKey(string(text))
	if err != nil {
		return err
	}
	*value = parsed
	return nil
}

// Mode represents the corresponding Zig open enum; values outside the named constants are valid.
type Mode int32

const (
	// ModeOff corresponds to the Zig tag off.
	ModeOff Mode = 0
	// ModeOn corresponds to the Zig tag on.
	ModeOn Mode = 1
)

// String returns the Zig tag name.
func (value Mode) String() string {
	switch value {
	case ModeOff:
		return "off"
	case ModeOn:
		return "on"
	default:
		return "Mode(" + strconv.Itoa(int(value)) + ")"
	}
}
