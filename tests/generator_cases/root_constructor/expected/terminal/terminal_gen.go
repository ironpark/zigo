// Code generated by zigo. DO NOT EDIT.

// Package terminal provides Go bindings generated by zigo.
package terminal

import (
	"runtime"

	"example.com/zigo/terminal/internal/raw"
)


// NewTerminal
// Opens a terminal the caller owns.
// The caller must call Close on the returned handle.
// Native failures are returned as generated error values.
func NewTerminal(columns uint32) (*Terminal, error) {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	result, code := raw.NewTerminal(columns)
	if code != 0 {
		return nil, errorForCode("NewTerminal", code)
	}
	return newTerminal(result), nil
}

// Resize
// Resizes the terminal.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (t *Terminal) Resize(columns uint32) error {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Terminal.Resize receiver", t)
	if err != nil {
		return err
	}
	defer t.zigoRelease()
	code := raw.TerminalResize(ptr, columns)
	if code != 0 {
		return zigoPoisonAfterPanic(errorForCode("Terminal.Resize", code), t)
	}
	return nil
}

// Render
// Renders the screen into memory the caller owns.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (t *Terminal) Render() (string, error) {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Terminal.Render receiver", t)
	if err != nil {
		return "", err
	}
	defer t.zigoRelease()
	result, code := raw.TerminalRender(ptr)
	if code != 0 {
		return "", zigoPoisonAfterPanic(errorForCode("Terminal.Render", code), t)
	}
	return string(result), nil
}
