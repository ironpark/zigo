// Code generated by zigo. DO NOT EDIT.
const std = @import("std");
const target = @import("zigo_target");

extern fn zg_panic_bridge(message: [*]const u8, length: usize) callconv(.c) noreturn;
fn panicHandler(message: []const u8, _: ?usize) noreturn {
    zg_panic_bridge(message.ptr, message.len);
}
pub const panic = std.debug.FullPanic(panicHandler);

export fn zg_new_terminal_impl(columns: u32, out_result: **target.Terminal) i32 {
    const result = target.newTerminal(columns) catch |err| return switch (err) {
        error.Invalid => 1,
    };
    out_result.* = result;
    return 0;
}
export fn zg_terminal_free_terminal_impl(self: *target.Terminal) i32 {
    target.freeTerminal(std.heap.smp_allocator, self);
    return 0;
}
export fn zg_terminal_resize_impl(self: *target.Terminal, columns: u32) i32 {
    target.Terminal.setSize(self, columns);
    return 0;
}
export fn zg_terminal_render_impl(self: *target.Terminal, out_result_ptr: *[*c]const u8, out_result_len: *usize) i32 {
    const result = target.Terminal.render(self);
    out_result_ptr.* = result.ptr;
    out_result_len.* = result.len;
    return 0;
}
export fn zg_free_string_impl(str_ptr: [*c]const u8, str_len: usize) void {
    target.freeString(std.heap.smp_allocator, if (str_len == 0) &.{} else str_ptr[0..str_len]);
}
