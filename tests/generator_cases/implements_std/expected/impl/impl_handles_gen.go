// Code generated by zigo. DO NOT EDIT.

package impl

import (
	"sync"
	"unsafe"
)

// Stream represents a native Zig handle.
type Stream struct {
	ptr    unsafe.Pointer
	mu     sync.Mutex
	active int
	closed bool
	poison *NativePanicError
}

// zigoAcquire pins s open for one native call and hands back its pointer;
// the call ends with zigoRelease. A nil, closed, or poisoned handle is the error.
func (s *Stream) zigoAcquire(operation string) (unsafe.Pointer, error) {
	if s == nil {
		return nil, &HandleError{Operation: operation}
	}
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.closed || s.ptr == nil {
		return nil, &HandleError{Operation: operation}
	}
	if s.poison != nil {
		return nil, s.poison.poisoned(operation)
	}
	s.active++
	return s.ptr, nil
}

func (s *Stream) zigoRelease() {
	if s == nil {
		return
	}
	s.mu.Lock()
	s.active--
	s.mu.Unlock()
}

// zigoPoison marks s unusable: a Zig panic unwound through native frames
// without running their defers, so the state behind it is unknown.
func (s *Stream) zigoPoison(cause *NativePanicError) {
	if s == nil {
		return
	}
	s.mu.Lock()
	defer s.mu.Unlock()
	if s.poison == nil {
		s.poison = cause
	}
}

// Buffer represents a native Zig handle.
type Buffer struct {
	ptr    unsafe.Pointer
	mu     sync.Mutex
	active int
	closed bool
	poison *NativePanicError
}

// zigoAcquire pins b open for one native call and hands back its pointer;
// the call ends with zigoRelease. A nil, closed, or poisoned handle is the error.
func (b *Buffer) zigoAcquire(operation string) (unsafe.Pointer, error) {
	if b == nil {
		return nil, &HandleError{Operation: operation}
	}
	b.mu.Lock()
	defer b.mu.Unlock()
	if b.closed || b.ptr == nil {
		return nil, &HandleError{Operation: operation}
	}
	if b.poison != nil {
		return nil, b.poison.poisoned(operation)
	}
	b.active++
	return b.ptr, nil
}

func (b *Buffer) zigoRelease() {
	if b == nil {
		return
	}
	b.mu.Lock()
	b.active--
	b.mu.Unlock()
}

// zigoPoison marks b unusable: a Zig panic unwound through native frames
// without running their defers, so the state behind it is unknown.
func (b *Buffer) zigoPoison(cause *NativePanicError) {
	if b == nil {
		return
	}
	b.mu.Lock()
	defer b.mu.Unlock()
	if b.poison == nil {
		b.poison = cause
	}
}
