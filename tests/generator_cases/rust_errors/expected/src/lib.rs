// Code generated by zigo. DO NOT EDIT.

//! Rust bindings for `calculator`, generated by zigo.

pub mod raw;

mod error;
pub use error::{Error, ErrorKind};

/// Divides two integers. Returns error.DivideByZero when the divisor is zero.
///
/// # Errors
///
/// - [`ErrorKind::DivideByZero`]
pub fn divide(numerator: i32, denominator: i32) -> Result<i32, Error> {
    let (result, code) = raw::divide(numerator, denominator);
    if code != 0 {
        return Err(Error::from_code("divide", code));
    }
    Ok(result)
}

/// Reports whether every byte is printable ASCII.
///
/// # Errors
///
/// - [`ErrorKind::NotPrintable`]
pub fn check_printable(text: &str) -> Result<(), Error> {
    let code = raw::check_printable(text);
    if code != 0 {
        return Err(Error::from_code("checkPrintable", code));
    }
    Ok(())
}
