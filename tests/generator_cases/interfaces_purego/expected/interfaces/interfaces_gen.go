// Code generated by zigo. DO NOT EDIT.

// Package interfaces provides Go bindings generated by zigo.
package interfaces

import (
	"runtime"

	"example.com/zigo/interfaces/internal/raw"
)

// LibraryError reports a native shared-library loading failure.
type LibraryError = raw.LibraryError

// LoadLibrary atomically loads all generated native entry points.
func LoadLibrary(path string) error { return raw.LoadLibrary(path) }

// LibraryLoaded reports whether the native call surface is ready.
func LibraryLoaded() bool { return raw.LibraryLoaded() }

// DefaultLibraryName is the installed shared-library basename for the running platform.
// LoadLibrary falls back to it when no explicit path and no ZIGO_LIBRARY_PATH are set.
var DefaultLibraryName = raw.DefaultLibraryName


// NewIntBatch creates a caller-owned IntBatch.
// The caller must call Close on the returned handle.
// Native failures are returned as generated error values.
func NewIntBatch() (*IntBatch, error) {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	result, code := raw.IntBatchCreate()
	if code != 0 {
		return nil, errorForCode("NewIntBatch", code)
	}
	return newIntBatch(result), nil
}

// MustNewIntBatch calls NewIntBatch and panics with its typed error on failure.
func MustNewIntBatch() *IntBatch { return zigoMust(NewIntBatch()) }

// Len reports how many values are staged.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (i *IntBatch) Len() (uint, error) {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("IntBatch.Len receiver", i)
	if err != nil {
		return 0, err
	}
	defer i.zigoRelease()
	result, code := raw.IntBatchLen(ptr)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(errorForCode("IntBatch.Len", code), i)
	}
	return result, nil
}

// MustLen calls Len and panics with its typed error on failure.
func (i *IntBatch) MustLen() uint { return zigoMust(i.Len()) }

// Clear calls the Zig function IntBatch.clear.
// It returns *HandleError if a required handle is nil or closed.
// Native failures are returned as generated error values.
func (i *IntBatch) Clear(keep uint) error {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("IntBatch.Clear receiver", i)
	if err != nil {
		return err
	}
	defer i.zigoRelease()
	code := raw.IntBatchClear(ptr, keep)
	if code != 0 {
		return zigoPoisonAfterPanic(errorForCode("IntBatch.Clear", code), i)
	}
	return nil
}

// MustClear calls Clear and panics with its typed error on failure.
func (i *IntBatch) MustClear(keep uint) { _ = zigoMust(struct{}{}, i.Clear(keep)) }

// Push calls the Zig function IntBatch.push.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (i *IntBatch) Push(value int32) error {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("IntBatch.Push receiver", i)
	if err != nil {
		return err
	}
	defer i.zigoRelease()
	code := raw.IntBatchPush(ptr, value)
	if code != 0 {
		return zigoPoisonAfterPanic(errorForCode("IntBatch.Push", code), i)
	}
	return nil
}

// MustPush calls Push and panics with its typed error on failure.
func (i *IntBatch) MustPush(value int32) { _ = zigoMust(struct{}{}, i.Push(value)) }

// NewFloatBatch creates a caller-owned FloatBatch.
// The caller must call Close on the returned handle.
// Native failures are returned as generated error values.
func NewFloatBatch() (*FloatBatch, error) {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	result, code := raw.FloatBatchCreate()
	if code != 0 {
		return nil, errorForCode("NewFloatBatch", code)
	}
	return newFloatBatch(result), nil
}

// MustNewFloatBatch calls NewFloatBatch and panics with its typed error on failure.
func MustNewFloatBatch() *FloatBatch { return zigoMust(NewFloatBatch()) }

// Len calls the Zig function FloatBatch.len.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (f *FloatBatch) Len() (uint, error) {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("FloatBatch.Len receiver", f)
	if err != nil {
		return 0, err
	}
	defer f.zigoRelease()
	result, code := raw.FloatBatchLen(ptr)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(errorForCode("FloatBatch.Len", code), f)
	}
	return result, nil
}

// MustLen calls Len and panics with its typed error on failure.
func (f *FloatBatch) MustLen() uint { return zigoMust(f.Len()) }

// Clear calls the Zig function FloatBatch.clear.
// It returns *HandleError if a required handle is nil or closed.
// Native failures are returned as generated error values.
func (f *FloatBatch) Clear(retain uint) error {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("FloatBatch.Clear receiver", f)
	if err != nil {
		return err
	}
	defer f.zigoRelease()
	code := raw.FloatBatchClear(ptr, retain)
	if code != 0 {
		return zigoPoisonAfterPanic(errorForCode("FloatBatch.Clear", code), f)
	}
	return nil
}

// MustClear calls Clear and panics with its typed error on failure.
func (f *FloatBatch) MustClear(retain uint) { _ = zigoMust(struct{}{}, f.Clear(retain)) }

// Push calls the Zig function FloatBatch.push.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (f *FloatBatch) Push(value float64) error {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("FloatBatch.Push receiver", f)
	if err != nil {
		return err
	}
	defer f.zigoRelease()
	code := raw.FloatBatchPush(ptr, value)
	if code != 0 {
		return zigoPoisonAfterPanic(errorForCode("FloatBatch.Push", code), f)
	}
	return nil
}

// MustPush calls Push and panics with its typed error on failure.
func (f *FloatBatch) MustPush(value float64) { _ = zigoMust(struct{}{}, f.Push(value)) }

// Size calls the Zig function Window.size.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (w *Window) Size() (uint, error) {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Window.Size receiver", w)
	if err != nil {
		return 0, err
	}
	defer w.zigoRelease()
	result, code := raw.WindowSize(ptr)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(errorForCode("Window.Size", code), w)
	}
	return result, nil
}

// MustSize calls Size and panics with its typed error on failure.
func (w *Window) MustSize() uint { return zigoMust(w.Size()) }

// Window calls the Zig function IntBatch.window.
// The returned reference remains valid only while its parent handle remains open.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (i *IntBatch) Window() (*Window, error) {
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("IntBatch.Window receiver", i)
	if err != nil {
		return nil, err
	}
	defer i.zigoRelease()
	result, code := raw.IntBatchWindow(ptr)
	if code != 0 {
		return nil, zigoPoisonAfterPanic(errorForCode("IntBatch.Window", code), i)
	}
	return newBorrowedWindow(result, i), nil
}

// MustWindow calls Window and panics with its typed error on failure.
func (i *IntBatch) MustWindow() *Window { return zigoMust(i.Window()) }
