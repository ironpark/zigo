// Code generated by zigo. DO NOT EDIT.

// Package impl provides Go bindings generated by zigo.
package impl

import (
	"io"
	"unsafe"

	"example.com/zigo/impl/internal/raw"
)

// LibraryError reports a native shared-library loading failure.
type LibraryError = raw.LibraryError

// LoadLibrary atomically loads all generated native entry points.
func LoadLibrary(path string) error { return raw.LoadLibrary(path) }

// LibraryLoaded reports whether the native call surface is ready.
func LibraryLoaded() bool { return raw.LibraryLoaded() }

// DefaultLibraryName is the installed shared-library basename for the running platform.
// LoadLibrary falls back to it when no explicit path and no ZIGO_LIBRARY_PATH are set.
var DefaultLibraryName = raw.DefaultLibraryName


// Feed calls the Zig function Stream.feed.
// It returns *HandleError if a required handle is nil or closed.
// Native failures are returned as generated error values.
func (s *Stream) Feed(bytes []byte) error {
	ptr, err := zigoCheckedPointer("Stream.Feed receiver", s)
	if err != nil {
		return err
	}
	defer s.zigoRelease()
	code := raw.StreamFeed(ptr, bytes)
	if code != 0 {
		return zigoPoisonAfterPanic(zigoErrorForCode("Stream.Feed", code), s)
	}
	return nil
}

// Write calls Feed, satisfying io.Writer.
// The method takes the whole of p, so the count is len(p) whenever it succeeds.
func (s *Stream) Write(p []byte) (int, error) {
	if err := s.Feed(p); err != nil {
		return 0, err
	}
	return len(p), nil
}

// WriteString calls Feed, satisfying io.StringWriter.
// The method takes the whole of str, so the count is len(str) whenever it succeeds.
// The method takes bytes, so str lends its own, without a copy; native reads them during the call only.
func (s *Stream) WriteString(str string) (int, error) {
	zigoBytes := unsafe.Slice(unsafe.StringData(str), len(str))
	if err := s.Feed(zigoBytes); err != nil {
		return 0, err
	}
	return len(str), nil
}

// Push calls the Zig function Buffer.push.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (b *Buffer) Push(bytes []byte) (uint, error) {
	ptr, err := zigoCheckedPointer("Buffer.Push receiver", b)
	if err != nil {
		return 0, err
	}
	defer b.zigoRelease()
	result, code := raw.BufferPush(ptr, bytes)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(zigoErrorForCode("Buffer.Push", code), b)
	}
	return result, nil
}

// Write calls Push, satisfying io.Writer.
// The count is what the method reports; a count short of len(p) without an error is io.ErrShortWrite.
func (b *Buffer) Write(p []byte) (int, error) {
	n, err := b.Push(p)
	if err != nil {
		return 0, err
	}
	if int(n) < len(p) {
		return int(n), io.ErrShortWrite
	}
	return int(n), nil
}

// AppendString calls the Zig function Stream.appendString.
// It returns *HandleError if a required handle is nil or closed.
// Native failures are returned as generated error values.
func (s *Stream) AppendString(text string) error {
	ptr, err := zigoCheckedPointer("Stream.AppendString receiver", s)
	if err != nil {
		return err
	}
	defer s.zigoRelease()
	code := raw.StreamAppendString(ptr, text)
	if code != 0 {
		return zigoPoisonAfterPanic(zigoErrorForCode("Stream.AppendString", code), s)
	}
	return nil
}

// PushString calls the Zig function Buffer.pushString.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (b *Buffer) PushString(bytes string) (uint, error) {
	ptr, err := zigoCheckedPointer("Buffer.PushString receiver", b)
	if err != nil {
		return 0, err
	}
	defer b.zigoRelease()
	result, code := raw.BufferPushString(ptr, bytes)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(zigoErrorForCode("Buffer.PushString", code), b)
	}
	return result, nil
}

// WriteString calls PushString, satisfying io.StringWriter.
// The count is what the method reports; a count short of len(s) without an error is io.ErrShortWrite.
func (b *Buffer) WriteString(s string) (int, error) {
	n, err := b.PushString(s)
	if err != nil {
		return 0, err
	}
	if int(n) < len(s) {
		return int(n), io.ErrShortWrite
	}
	return int(n), nil
}

// Drain calls the Zig function Buffer.drain.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (b *Buffer) Drain(dst []byte) (uint, error) {
	ptr, err := zigoCheckedPointer("Buffer.Drain receiver", b)
	if err != nil {
		return 0, err
	}
	defer b.zigoRelease()
	result, code := raw.BufferDrain(ptr, dst)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(zigoErrorForCode("Buffer.Drain", code), b)
	}
	return result, nil
}

// Read calls Drain, satisfying io.Reader.
// A call that fills nothing while p has room reports io.EOF.
func (b *Buffer) Read(p []byte) (int, error) {
	n, err := b.Drain(p)
	if err != nil {
		return 0, err
	}
	if n == 0 && len(p) > 0 {
		return 0, io.EOF
	}
	return int(n), nil
}

// Dump calls the Zig function Stream.dump.
// It returns *HandleError if a required handle is nil or closed.
// Native failures are returned as generated error values.
// A panic in a Go callback is rethrown as *CallbackPanicError once the native call returns.
func (s *Stream) Dump(w io.Writer) error {
	if w == nil {
		return &StreamError{Operation: "Stream.Dump", Parameter: "w", Err: ErrNilStream}
	}
	ptr, err := zigoCheckedPointer("Stream.Dump receiver", s)
	if err != nil {
		return err
	}
	defer s.zigoRelease()
	wHandle := zigoNewWriterStreamHandle(w)
	defer zigoDeleteCallbackHandle(wHandle)
	code := raw.StreamDump(ptr, raw.StreamWriterCallbackPointer(), uintptr(wHandle))
	if zigoCallbackPanicPending() {
		zigoRethrowCallbackPanic("Stream.Dump", wHandle)
	}
	if err := zigoStreamError("Stream.Dump", "w", wHandle); err != nil {
		return err
	}
	if code != 0 {
		return zigoPoisonAfterPanic(zigoErrorForCode("Stream.Dump", code), s)
	}
	return nil
}

// WriteTo calls Dump, satisfying io.WriterTo.
// The count is what w received during the call.
func (s *Stream) WriteTo(w io.Writer) (int64, error) {
	counting := &zigoCountingWriter{w: w}
	err := s.Dump(counting)
	return counting.n, err
}

// Load calls the Zig function Stream.load.
// It returns *HandleError if a required handle is nil or closed.
// Native failures are returned as generated error values.
// A panic in a Go callback is rethrown as *CallbackPanicError once the native call returns.
func (s *Stream) Load(r io.Reader) (uint, error) {
	if r == nil {
		return 0, &StreamError{Operation: "Stream.Load", Parameter: "r", Err: ErrNilStream}
	}
	ptr, err := zigoCheckedPointer("Stream.Load receiver", s)
	if err != nil {
		return 0, err
	}
	defer s.zigoRelease()
	rHandle := zigoNewReaderStreamHandle(r)
	defer zigoDeleteCallbackHandle(rHandle)
	rData := zigoReaderBytes(r)
	result, code := raw.StreamLoad(ptr, raw.StreamReaderCallbackPointer(), uintptr(rHandle), rData)
	if zigoCallbackPanicPending() {
		zigoRethrowCallbackPanic("Stream.Load", rHandle)
	}
	if err := zigoStreamError("Stream.Load", "r", rHandle); err != nil {
		return 0, err
	}
	if code != 0 {
		return 0, zigoPoisonAfterPanic(zigoErrorForCode("Stream.Load", code), s)
	}
	return result, nil
}

// ReadFrom calls Load, satisfying io.ReaderFrom.
// The count is what the method reports.
func (s *Stream) ReadFrom(r io.Reader) (int64, error) {
	n, err := s.Load(r)
	if err != nil {
		return 0, err
	}
	return int64(n), nil
}

// Render calls the Zig function Buffer.render.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
// A panic in a Go callback is rethrown as *CallbackPanicError once the native call returns.
func (b *Buffer) Render(w io.Writer) (uint32, error) {
	if w == nil {
		return 0, &StreamError{Operation: "Buffer.Render", Parameter: "w", Err: ErrNilStream}
	}
	ptr, err := zigoCheckedPointer("Buffer.Render receiver", b)
	if err != nil {
		return 0, err
	}
	defer b.zigoRelease()
	wHandle := zigoNewWriterStreamHandle(w)
	defer zigoDeleteCallbackHandle(wHandle)
	result, code := raw.BufferRender(ptr, raw.StreamWriterCallbackPointer(), uintptr(wHandle))
	if zigoCallbackPanicPending() {
		zigoRethrowCallbackPanic("Buffer.Render", wHandle)
	}
	if err := zigoStreamError("Buffer.Render", "w", wHandle); err != nil {
		return 0, err
	}
	if code != 0 {
		return 0, zigoPoisonAfterPanic(zigoErrorForCode("Buffer.Render", code), b)
	}
	return result, nil
}

// WriteTo calls Render, satisfying io.WriterTo.
// The count is what the method reports.
func (b *Buffer) WriteTo(w io.Writer) (int64, error) {
	n, err := b.Render(w)
	if err != nil {
		return 0, err
	}
	return int64(n), nil
}

// Fill calls the Zig function Buffer.fill.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
// A panic in a Go callback is rethrown as *CallbackPanicError once the native call returns.
func (b *Buffer) Fill(r io.Reader) error {
	if r == nil {
		return &StreamError{Operation: "Buffer.Fill", Parameter: "r", Err: ErrNilStream}
	}
	ptr, err := zigoCheckedPointer("Buffer.Fill receiver", b)
	if err != nil {
		return err
	}
	defer b.zigoRelease()
	rHandle := zigoNewReaderStreamHandle(r)
	defer zigoDeleteCallbackHandle(rHandle)
	rData := zigoReaderBytes(r)
	code := raw.BufferFill(ptr, raw.StreamReaderCallbackPointer(), uintptr(rHandle), rData)
	if zigoCallbackPanicPending() {
		zigoRethrowCallbackPanic("Buffer.Fill", rHandle)
	}
	if err := zigoStreamError("Buffer.Fill", "r", rHandle); err != nil {
		return err
	}
	if code != 0 {
		return zigoPoisonAfterPanic(zigoErrorForCode("Buffer.Fill", code), b)
	}
	return nil
}

// ReadFrom calls Fill, satisfying io.ReaderFrom.
// The count is what r handed over during the call.
func (b *Buffer) ReadFrom(r io.Reader) (int64, error) {
	counting := &zigoCountingReader{r: r}
	err := b.Fill(counting)
	return counting.n, err
}
