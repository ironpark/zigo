// Code generated by zigo. DO NOT EDIT.
const std = @import("std");
const target = @import("zigo_target");

extern fn zg_panic_bridge(message: [*]const u8, length: usize) callconv(.c) noreturn;
fn panicHandler(message: []const u8, _: ?usize) noreturn {
    zg_panic_bridge(message.ptr, message.len);
}
pub const panic = std.debug.FullPanic(panicHandler);

export fn zg_width_impl(cp: u32, out_result: *i8) i32 {
    if (cp > std.math.maxInt(u21)) @panic("zigo: argument `cp` is out of range for u21");
    const result = target.width(@intCast(cp));
    out_result.* = result;
    return 0;
}
export fn zg_checked_width_impl(cp: u32, out_result: *i8) i32 {
    if (cp > std.math.maxInt(u21)) @panic("zigo: argument `cp` is out of range for u21");
    const result = target.checkedWidth(@intCast(cp)) catch |err| return switch (err) {
        error.NotPrintable => 1,
    };
    out_result.* = result;
    return 0;
}
export fn zg_is_printable_impl(cp: u32, out_result: *u8) i32 {
    const result = target.isPrintable(cp) catch |err| return switch (err) {
        error.NotPrintable => 1,
    };
    out_result.* = @intFromBool(result);
    return 0;
}
