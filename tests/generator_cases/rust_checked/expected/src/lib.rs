// Code generated by zigo. DO NOT EDIT.

//! Rust bindings for `text`, generated by zigo.

pub mod raw;

mod error;
pub use error::{Error, ErrorKind};

/// Reports the display width of a codepoint. Infallible in Zig, but a
/// narrow-integer parameter gives it a status channel anyway: the shim
/// range-checks the argument and a panic needs somewhere to be reported.
pub fn width(cp: u32) -> i8 {
    let (result, code) = raw::width(cp);
    if code != 0 {
        raw::panic_native("width", code);
    }
    result
}

/// The same shape with a declared error set, so this one is a Result.
///
/// # Errors
///
/// - [`ErrorKind::NotPrintable`]
pub fn checked_width(cp: u32) -> Result<i8, Error> {
    let (result, code) = raw::checked_width(cp);
    if code != 0 {
        return Err(Error::from_code("checkedWidth", code));
    }
    Ok(result)
}

/// A bool behind a declared error set, which used to arrive as u8.
///
/// # Errors
///
/// - [`ErrorKind::NotPrintable`]
pub fn is_printable(cp: u32) -> Result<bool, Error> {
    let (result, code) = raw::is_printable(cp);
    if code != 0 {
        return Err(Error::from_code("isPrintable", code));
    }
    Ok(result != 0)
}
