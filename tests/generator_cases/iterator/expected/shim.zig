// Code generated by zigo. DO NOT EDIT.
const std = @import("std");
const target = @import("zigo_target");

extern fn zg_panic_bridge(message: [*]const u8, length: usize) callconv(.c) noreturn;
fn panicHandler(message: []const u8, _: ?usize) noreturn {
    zg_panic_bridge(message.ptr, message.len);
}
pub const panic = std.debug.FullPanic(panicHandler);

export fn zg_cursor_next_impl(self: *target.Cursor, out_result_has: *u8, out_result: *i64) i32 {
    const result = target.Cursor.next(self);
    if (result) |zigo_value| {
        out_result_has.* = 1;
        out_result.* = zigo_value;
    } else {
        out_result_has.* = 0;
    }
    return 0;
}
export fn zg_cursor_next_checked_impl(self: *target.Cursor, out_result_has: *u8, out_result: *u8) i32 {
    const result = target.Cursor.nextChecked(self) catch |err| return switch (err) {
        error.Exhausted => 1,
    };
    if (result) |zigo_value| {
        out_result_has.* = 1;
        out_result.* = @intFromEnum(zigo_value);
    } else {
        out_result_has.* = 0;
    }
    return 0;
}
export fn zg_cursor_reset_impl(self: *target.Cursor) i32 {
    target.Cursor.reset(self);
    return 0;
}
