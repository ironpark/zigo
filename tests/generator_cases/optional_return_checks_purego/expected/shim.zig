// Code generated by zigo. DO NOT EDIT.
const std = @import("std");
const target = @import("zigo_target");

extern fn zg_panic_bridge(message: [*]const u8, length: usize) callconv(.c) noreturn;
fn panicHandler(message: []const u8, _: ?usize) noreturn {
    zg_panic_bridge(message.ptr, message.len);
}
pub const panic = std.debug.FullPanic(panicHandler);

export fn zg_terminal_password_impl(self: *target.Terminal, out_result_has: *u8, out_result: *i32) i32 {
    const result = target.Terminal.password(self);
    if (result) |zigo_value| {
        out_result_has.* = 1;
        out_result.* = zigo_value;
    } else {
        out_result_has.* = 0;
    }
    return 0;
}
export fn zg_checked_level_impl(level: u8, out_result_has: *u8, out_result: *i32) i32 {
    if (level > std.math.maxInt(u7)) @panic("zigo: argument `level` is out of range for u7");
    const result = target.checkedLevel(@intCast(level)) catch |err| return switch (err) {
        error.Invalid => 1,
    };
    if (result) |zigo_value| {
        out_result_has.* = 1;
        out_result.* = zigo_value;
    } else {
        out_result_has.* = 0;
    }
    return 0;
}
export fn zg_terminal_value_impl(terminal: ?*const target.Terminal, out_result_has: *u8, out_result: *i32) i32 {
    const result = target.terminalValue(terminal);
    if (result) |zigo_value| {
        out_result_has.* = 1;
        out_result.* = zigo_value;
    } else {
        out_result_has.* = 0;
    }
    return 0;
}
