// Code generated by zigo. DO NOT EDIT.
#include <setjmp.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "zigo_cb.h"

// ELF and Mach-O export every non-static symbol of a shared library;
// COFF exports nothing without an explicit annotation, so a DLL built
// without this would load and then resolve none of its entry points.
#ifndef ZIGO_EXPORT
#if defined(_WIN32)
#define ZIGO_EXPORT __declspec(dllexport)
#else
#define ZIGO_EXPORT
#endif
#endif

static _Thread_local jmp_buf zg_panic_env;
static _Thread_local int zg_panic_active;
static _Thread_local char zg_panic_message[1024];

// A panic with nowhere to be reported is what Zig says it is: fatal.
// The message is written out first so the process does not die silent.
_Noreturn static void zg_panic_fatal(void) {
    fputs("zigo: native panic: ", stderr);
    fputs(zg_panic_message, stderr);
    fputc('\n', stderr);
    fflush(stderr);
    abort();
}

void zg_panic_bridge(const uint8_t *message, size_t length) {
    size_t count = length < sizeof(zg_panic_message) - 1 ? length : sizeof(zg_panic_message) - 1;
    memcpy(zg_panic_message, message, count);
    zg_panic_message[count] = '\0';
    if (zg_panic_active) longjmp(zg_panic_env, 1);
    zg_panic_fatal();
}

ZIGO_EXPORT const char *zg_last_error_message(void) { return zg_panic_message; }

int32_t zg_hub_create_impl(size_t userdata, zg_hub * * out_result);
ZIGO_EXPORT int32_t zg_hub_create(size_t userdata, zg_hub * * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_hub_create_impl(userdata, out_result);
    zg_panic_active = 0;
    return result;
}

int32_t zg_hub_run_impl(zg_hub * self, int32_t value, int32_t * out_result);
ZIGO_EXPORT int32_t zg_hub_run(zg_hub * self, int32_t value, int32_t * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_hub_run_impl(self, value, out_result);
    zg_panic_active = 0;
    return result;
}

int32_t zg_hub_deinit_impl(zg_hub * self);
ZIGO_EXPORT int32_t zg_hub_deinit(zg_hub * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_hub_deinit_impl(self);
    zg_panic_active = 0;
    return result;
}

int32_t zg_apply_impl(int32_t value, size_t userdata);
ZIGO_EXPORT int32_t zg_apply(int32_t value, size_t userdata) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        zg_panic_fatal();
    }
    int32_t result = zg_apply_impl(value, userdata);
    zg_panic_active = 0;
    return result;
}

void zg_notify_impl(int32_t value, size_t userdata);
ZIGO_EXPORT void zg_notify(int32_t value, size_t userdata) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        zg_panic_fatal();
    }
zg_notify_impl(value, userdata);
    zg_panic_active = 0;
}
