// Code generated by zigo. DO NOT EDIT.
const std = @import("std");
const target = @import("zigo_target");

extern fn zg_panic_bridge(message: [*]const u8, length: usize) callconv(.c) noreturn;
fn panicHandler(message: []const u8, _: ?usize) noreturn {
    zg_panic_bridge(message.ptr, message.len);
}
pub const panic = std.debug.FullPanic(panicHandler);

export fn zg_configure_impl(config: *const target.Config) void {
    target.configure(config.*);
}
export fn zg_default_config_impl(out_result: *target.Config) void {
    out_result.* = target.defaultConfig();
}
export fn zg_translate_impl(origin: *const target.Point, dx: i16, out_result: *target.Point) void {
    out_result.* = target.translate(origin.*, dx);
}
export fn zg_load_impl(out_result: *target.Config) i32 {
    const result = target.load() catch |err| return switch (err) {
        error.Invalid => 1,
    };
    out_result.* = result;
    return 0;
}
export fn zg_accept_points_impl(values_ptr: [*c]const target.Point, values_len: usize) void {
    target.acceptPoints(if (values_len == 0) &.{} else values_ptr[0..values_len]);
}
export fn zg_fill_points_impl(output_ptr: [*c]target.Point, output_len: usize, out_result: *usize) i32 {
    const result = target.fillPoints(if (output_len == 0) &.{} else output_ptr[0..output_len]) catch |err| return switch (err) {
        error.Invalid => 1,
    };
    out_result.* = result;
    return 0;
}
export fn zg_fill_all_points_impl(output_ptr: [*c]target.Point, output_len: usize, output_written: *usize) void {
    target.fillAllPoints(if (output_len == 0) &.{} else output_ptr[0..output_len]);
    output_written.* = output_len;
}
export fn zg_accept_configs_impl(values_ptr: [*c]const target.Config, values_len: usize) void {
    target.acceptConfigs(if (values_len == 0) &.{} else values_ptr[0..values_len]);
}
export fn zg_fill_configs_impl(output_ptr: [*c]target.Config, output_len: usize) usize {
    return target.fillConfigs(if (output_len == 0) &.{} else output_ptr[0..output_len]);
}
export fn zg_configs_impl(out_result_ptr: *[*c]const target.Config, out_result_len: *usize) void {
    const result = target.configs();
    out_result_ptr.* = result.ptr;
    out_result_len.* = result.len;
}
export fn zg_points_impl(out_result_ptr: *[*c]const target.Point, out_result_len: *usize) void {
    const result = target.points();
    out_result_ptr.* = result.ptr;
    out_result_len.* = result.len;
}
export fn zg_points_checked_impl(out_result_ptr: *[*c]const target.Point, out_result_len: *usize) i32 {
    const result = target.pointsChecked() catch |err| return switch (err) {
        error.Invalid => 1,
    };
    out_result_ptr.* = result.ptr;
    out_result_len.* = result.len;
    return 0;
}

/// Fails this compile when a layout zigo reflected on the build host does
/// not describe the compilation target. The usual cause is a C type whose
/// width varies by target -- `c_long` and `c_ulong` are 4 bytes on Windows
/// and 8 bytes on Linux and macOS, and `c_longdouble` varies too. Use a
/// fixed-width type in the binding surface, or generate on the target.
fn zigoAbiGuard(comptime what: []const u8, comptime reflected: usize, comptime actual: usize) void {
    if (reflected != actual) @compileError(std.fmt.comptimePrint(
        "zigo ABI guard: {s} is {d} on this target, but zigo reflected {d} on the build host. " ++
            "The generated C header and Go mirrors use the reflected layout, so this binding " ++
            "cannot be built for this target. A C type whose width varies by target, such as " ++
            "c_long or c_ulong, is the usual cause; replace it with a fixed-width type.",
        .{ what, actual, reflected },
    ));
}

comptime {
    zigoAbiGuard("@sizeOf(Point)", 4, @sizeOf(target.Point));
    zigoAbiGuard("@alignOf(Point)", 2, @alignOf(target.Point));
    zigoAbiGuard("@offsetOf(Point, \"x\")", 0, @offsetOf(target.Point, "x"));
    zigoAbiGuard("@offsetOf(Point, \"y\")", 2, @offsetOf(target.Point, "y"));
}

comptime {
    zigoAbiGuard("@sizeOf(Config)", 32, @sizeOf(target.Config));
    zigoAbiGuard("@alignOf(Config)", 8, @alignOf(target.Config));
    zigoAbiGuard("@offsetOf(Config, \"enabled\")", 0, @offsetOf(target.Config, "enabled"));
    zigoAbiGuard("@offsetOf(Config, \"width\")", 4, @offsetOf(target.Config, "width"));
    zigoAbiGuard("@offsetOf(Config, \"mode\")", 8, @offsetOf(target.Config, "mode"));
    zigoAbiGuard("@offsetOf(Config, \"ratio\")", 16, @offsetOf(target.Config, "ratio"));
    zigoAbiGuard("@offsetOf(Config, \"origin\")", 24, @offsetOf(target.Config, "origin"));
}
