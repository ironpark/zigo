// Code generated by zigo. DO NOT EDIT.
#include <setjmp.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "zigo_narrow_int.h"

// ELF and Mach-O export every non-static symbol of a shared library;
// COFF exports nothing without an explicit annotation, so a DLL built
// without this would load and then resolve none of its entry points.
#ifndef ZIGO_EXPORT
#if defined(_WIN32)
#define ZIGO_EXPORT __declspec(dllexport)
#else
#define ZIGO_EXPORT
#endif
#endif

static _Thread_local jmp_buf zg_panic_env;
static _Thread_local int zg_panic_active;
static _Thread_local char zg_panic_message[1024];

// A panic with nowhere to be reported is what Zig says it is: fatal.
// The message is written out first so the process does not die silent.
_Noreturn static void zg_panic_fatal(void) {
    fputs("zigo: native panic: ", stderr);
    fputs(zg_panic_message, stderr);
    fputc('\n', stderr);
    fflush(stderr);
    abort();
}

void zg_panic_bridge(const uint8_t *message, size_t length) {
    size_t count = length < sizeof(zg_panic_message) - 1 ? length : sizeof(zg_panic_message) - 1;
    memcpy(zg_panic_message, message, count);
    zg_panic_message[count] = '\0';
    if (zg_panic_active) longjmp(zg_panic_env, 1);
    zg_panic_fatal();
}

ZIGO_EXPORT const char *zg_last_error_message(void) { return zg_panic_message; }

int32_t zg_codepoint_width_impl(uint32_t cp, int8_t * out_result);
ZIGO_EXPORT int32_t zg_codepoint_width(uint32_t cp, int8_t * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_codepoint_width_impl(cp, out_result);
    zg_panic_active = 0;
    return result;
}

int32_t zg_clamp_offset_impl(int32_t offset, int32_t * out_result);
ZIGO_EXPORT int32_t zg_clamp_offset(int32_t offset, int32_t * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_clamp_offset_impl(offset, out_result);
    zg_panic_active = 0;
    return result;
}

int32_t zg_decode_impl(uint8_t byte, uint32_t * out_result);
ZIGO_EXPORT int32_t zg_decode(uint8_t byte, uint32_t * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_decode_impl(byte, out_result);
    zg_panic_active = 0;
    return result;
}

uint32_t zg_sum_codepoints_impl(const uint32_t * values_ptr, size_t values_len);
ZIGO_EXPORT uint32_t zg_sum_codepoints(const uint32_t * values_ptr, size_t values_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        zg_panic_fatal();
    }
    uint32_t result = zg_sum_codepoints_impl(values_ptr, values_len);
    zg_panic_active = 0;
    return result;
}

void zg_fill_codepoints_impl(uint32_t * values_ptr, size_t values_len, size_t * values_written);
ZIGO_EXPORT void zg_fill_codepoints(uint32_t * values_ptr, size_t values_len, size_t * values_written) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        zg_panic_fatal();
    }
zg_fill_codepoints_impl(values_ptr, values_len, values_written);
    zg_panic_active = 0;
}

void zg_take_codepoints_impl(const uint32_t * * out_result_ptr, size_t * out_result_len);
ZIGO_EXPORT void zg_take_codepoints(const uint32_t * * out_result_ptr, size_t * out_result_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        zg_panic_fatal();
    }
zg_take_codepoints_impl(out_result_ptr, out_result_len);
    zg_panic_active = 0;
}

void zg_free_codepoints_impl(const uint32_t * values_ptr, size_t values_len);
ZIGO_EXPORT void zg_free_codepoints(const uint32_t * values_ptr, size_t values_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        zg_panic_fatal();
    }
zg_free_codepoints_impl(values_ptr, values_len);
    zg_panic_active = 0;
}
