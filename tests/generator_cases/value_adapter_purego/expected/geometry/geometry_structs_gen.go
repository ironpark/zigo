// Code generated by zigo. DO NOT EDIT.

package geometry

import (
	"image"

	"example.com/zigo/geometry/internal/raw"
)

// Rect mirrors the Zig `extern struct` of the same name.
type Rect struct {
	// Min corresponds to the Zig field min.
	Min image.Point
	// Max corresponds to the Zig field max.
	Max image.Point
}

// zigoPointToRaw converts through the binding's `.go` adapter (pointToRaw).
func zigoPointToRaw(value image.Point) raw.PointData {
	return pointToRaw(value)
}

// zigoPointFromRaw converts through the binding's `.go` adapter (pointFromRaw).
func zigoPointFromRaw(value raw.PointData) image.Point {
	return pointFromRaw(value)
}

func zigoPointSliceToRaw(values []image.Point) []raw.PointData {
	result := make([]raw.PointData, len(values))
	for i := range values {
		result[i] = zigoPointToRaw(values[i])
	}
	return result
}

func zigoPointSliceFromRaw(values []raw.PointData) []image.Point {
	result := make([]image.Point, len(values))
	for i := range values {
		result[i] = zigoPointFromRaw(values[i])
	}
	return result
}

func zigoRectFromRaw(value raw.RectData) Rect {
	return Rect{
		Min: zigoPointFromRaw(value.Min),
		Max: zigoPointFromRaw(value.Max),
	}
}
