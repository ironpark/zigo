// Code generated by zigo. DO NOT EDIT.
const std = @import("std");
const target = @import("zigo_target");

extern fn zg_panic_bridge(message: [*]const u8, length: usize) callconv(.c) noreturn;
fn panicHandler(message: []const u8, _: ?usize) noreturn {
    zg_panic_bridge(message.ptr, message.len);
}
pub const panic = std.debug.FullPanic(panicHandler);
pub const std_options: std.Options = if (@hasDecl(target, "std_options")) target.std_options else .{};

export fn zg_translate_impl(origin: *const target.Point, dx: i16, out_result: *target.Point) void {
    out_result.* = target.translate(origin.*, dx);
}
export fn zg_shift_impl(origin: ?*const target.Point, out_result_has: *u8, out_result: *target.Point) i32 {
    const result = target.shift(if (origin) |zigo_origin| zigo_origin.* else null) catch |err| return switch (err) {
        error.Overflow => 1,
    };
    if (result) |zigo_value| {
        out_result_has.* = 1;
        out_result.* = zigo_value;
    } else {
        out_result_has.* = 0;
    }
    return 0;
}
export fn zg_accept_points_impl(values_ptr: [*c]const target.Point, values_len: usize) usize {
    return target.acceptPoints(if (values_len == 0) &.{} else values_ptr[0..values_len]);
}
export fn zg_corners_impl(out_result_ptr: *[*c]const target.Point, out_result_len: *usize) void {
    const result = target.corners();
    out_result_ptr.* = result.ptr;
    out_result_len.* = result.len;
}
export fn zg_bounds_impl(out_result: *target.Rect) void {
    out_result.* = target.bounds();
}
export fn zg_set_speed_impl(speed: u8) u8 {
    return @intFromEnum(target.setSpeed(@enumFromInt(speed)));
}
export fn zg_speeds_impl(values_ptr: [*c]const u8, values_len: usize, out_result_ptr: *[*c]const u8, out_result_len: *usize) void {
    const result = target.speeds(if (values_len == 0) &.{} else values_ptr[0..values_len]);
    out_result_ptr.* = result.ptr;
    out_result_len.* = result.len;
}
export fn zg_elapsed_impl(since: u64) u64 {
    return target.elapsed(since);
}
export fn zg_checked_elapsed_impl(strict: u8, out_result: *u64) i32 {
    const result = target.checkedElapsed(strict != 0) catch |err| return switch (err) {
        error.Overflow => 1,
    };
    out_result.* = result;
    return 0;
}

/// Fails this compile when a layout zigo reflected on the build host does
/// not describe the compilation target. The usual cause is a C type whose
/// width varies by target -- `c_long` and `c_ulong` are 4 bytes on Windows
/// and 8 bytes on Linux and macOS, and `c_longdouble` varies too. Use a
/// fixed-width type in the binding surface, or generate on the target.
fn zigoAbiGuard(comptime what: []const u8, comptime reflected: usize, comptime actual: usize) void {
    if (reflected != actual) @compileError(std.fmt.comptimePrint(
        "zigo ABI guard: {s} is {d} on this target, but zigo reflected {d} on the build host. " ++
            "The generated C header and Go mirrors use the reflected layout, so this binding " ++
            "cannot be built for this target. A C type whose width varies by target, such as " ++
            "c_long or c_ulong, is the usual cause; replace it with a fixed-width type.",
        .{ what, actual, reflected },
    ));
}

comptime {
    zigoAbiGuard("@sizeOf(Point)", 4, @sizeOf(target.Point));
    zigoAbiGuard("@alignOf(Point)", 2, @alignOf(target.Point));
    zigoAbiGuard("@offsetOf(Point, \"x\")", 0, @offsetOf(target.Point, "x"));
    zigoAbiGuard("@offsetOf(Point, \"y\")", 2, @offsetOf(target.Point, "y"));
}

comptime {
    zigoAbiGuard("@sizeOf(Rect)", 10, @sizeOf(target.Rect));
    zigoAbiGuard("@alignOf(Rect)", 2, @alignOf(target.Rect));
    zigoAbiGuard("@offsetOf(Rect, \"min\")", 0, @offsetOf(target.Rect, "min"));
    zigoAbiGuard("@offsetOf(Rect, \"max\")", 4, @offsetOf(target.Rect, "max"));
    zigoAbiGuard("@offsetOf(Rect, \"speed\")", 8, @offsetOf(target.Rect, "speed"));
}
