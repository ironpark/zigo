// Code generated by zigo. DO NOT EDIT.

// Package optional provides Go bindings generated by zigo.
package optional

import "example.com/zigo/optional/internal/raw"

// LibraryError reports a native shared-library loading failure.
type LibraryError = raw.LibraryError

// LoadLibrary atomically loads all generated native entry points.
func LoadLibrary(path string) error { return raw.LoadLibrary(path) }

// LibraryLoaded reports whether the native call surface is ready.
func LibraryLoaded() bool { return raw.LibraryLoaded() }

// DefaultLibraryName is the installed shared-library basename for the running platform.
// LoadLibrary falls back to it when no explicit path and no ZIGO_LIBRARY_PATH are set.
var DefaultLibraryName = raw.DefaultLibraryName


// Double calls the Zig function double.
// A nil value is the absent value.
// The bool result reports whether a value was present; the value before it is zero when it was not.
func Double(value *int32) (int32, bool) {
	zigoResult, zigoHas := raw.Double(value)
	return zigoResult, zigoHas
}

// Flag calls the Zig function flag.
// A nil value is the absent value.
func Flag(value *bool) bool {
	var valueRaw *uint8
	if value != nil {
		valueRawValue := zigoBoolToUint8(*value)
		valueRaw = &valueRawValue
	}
	return raw.Flag(valueRaw) != 0
}

// PickMode calls the Zig function pickMode.
// A nil value is the absent value.
// The bool result reports whether a value was present; the value before it is zero when it was not.
func PickMode(value *Mode) (Mode, bool) {
	var valueRaw *uint8
	if value != nil {
		valueRawValue := uint8(*value)
		valueRaw = &valueRawValue
	}
	zigoResult, zigoHas := raw.PickMode(valueRaw)
	return Mode(zigoResult), zigoHas
}

// CheckedDouble calls the Zig function checkedDouble.
// A nil value is the absent value.
// The bool result reports whether a value was present; the value before it is zero when it was not.
// Native failures are returned as generated error values.
func CheckedDouble(value *int32) (int32, bool, error) {
	result, zigoHas, code := raw.CheckedDouble(value)
	if code != 0 {
		return 0, false, zigoErrorForCode("CheckedDouble", code)
	}
	return result, zigoHas, nil
}

// ShiftPoint calls the Zig function shiftPoint.
// A nil origin is the absent value.
// The bool result reports whether a value was present; the value before it is zero when it was not.
func ShiftPoint(origin *Point) (Point, bool) {
	var originRaw *raw.PointData
	if origin != nil {
		originRawValue := zigoPointToRaw(*origin)
		originRaw = &originRawValue
	}
	zigoResult, zigoHas := raw.ShiftPoint(originRaw)
	return zigoPointFromRaw(zigoResult), zigoHas
}

// CheckedPoint calls the Zig function checkedPoint.
// A nil origin is the absent value.
// The bool result reports whether a value was present; the value before it is zero when it was not.
// Native failures are returned as generated error values.
func CheckedPoint(origin *Point) (Point, bool, error) {
	var originRaw *raw.PointData
	if origin != nil {
		originRawValue := zigoPointToRaw(*origin)
		originRaw = &originRawValue
	}
	result, zigoHas, code := raw.CheckedPoint(originRaw)
	if code != 0 {
		return Point{}, false, zigoErrorForCode("CheckedPoint", code)
	}
	return zigoPointFromRaw(result), zigoHas, nil
}
