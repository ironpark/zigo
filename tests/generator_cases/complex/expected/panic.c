// Code generated by zigo. DO NOT EDIT.
#include <setjmp.h>
#include <stddef.h>
#include <stdint.h>
#include <stdlib.h>
#include <string.h>
#include "zigo_pipeline.h"

// ELF and Mach-O export every non-static symbol of a shared library;
// COFF exports nothing without an explicit annotation, so a DLL built
// without this would load and then resolve none of its entry points.
#ifndef ZIGO_EXPORT
#if defined(_WIN32)
#define ZIGO_EXPORT __declspec(dllexport)
#else
#define ZIGO_EXPORT
#endif
#endif

static _Thread_local jmp_buf zg_panic_env;
static _Thread_local int zg_panic_active;
static _Thread_local char zg_panic_message[1024];

void zg_panic_bridge(const uint8_t *message, size_t length) {
    size_t count = length < sizeof(zg_panic_message) - 1 ? length : sizeof(zg_panic_message) - 1;
    memcpy(zg_panic_message, message, count);
    zg_panic_message[count] = '\0';
    if (zg_panic_active) longjmp(zg_panic_env, 1);
    abort();
}

ZIGO_EXPORT const char *zg_last_error_message(void) { return zg_panic_message; }

int32_t zg_int_batch_create_impl(zg_int_batch * * out_result);
ZIGO_EXPORT int32_t zg_int_batch_create(zg_int_batch * * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_int_batch_create_impl(out_result);
    zg_panic_active = 0;
    return result;
}

int32_t zg_int_batch_push_impl(zg_int_batch * self, int32_t value);
ZIGO_EXPORT int32_t zg_int_batch_push(zg_int_batch * self, int32_t value) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_int_batch_push_impl(self, value);
    zg_panic_active = 0;
    return result;
}

size_t zg_int_batch_len_impl(zg_int_batch * self);
ZIGO_EXPORT size_t zg_int_batch_len(zg_int_batch * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    size_t result = zg_int_batch_len_impl(self);
    zg_panic_active = 0;
    return result;
}

void zg_int_batch_deinit_impl(zg_int_batch * self);
ZIGO_EXPORT void zg_int_batch_deinit(zg_int_batch * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return;
    }
zg_int_batch_deinit_impl(self);
    zg_panic_active = 0;
}

int32_t zg_float_batch_create_impl(zg_float_batch * * out_result);
ZIGO_EXPORT int32_t zg_float_batch_create(zg_float_batch * * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_float_batch_create_impl(out_result);
    zg_panic_active = 0;
    return result;
}

int32_t zg_float_batch_push_impl(zg_float_batch * self, double p0);
ZIGO_EXPORT int32_t zg_float_batch_push(zg_float_batch * self, double p0) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_float_batch_push_impl(self, p0);
    zg_panic_active = 0;
    return result;
}

size_t zg_float_batch_len_impl(zg_float_batch * self);
ZIGO_EXPORT size_t zg_float_batch_len(zg_float_batch * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    size_t result = zg_float_batch_len_impl(self);
    zg_panic_active = 0;
    return result;
}

void zg_float_batch_deinit_impl(zg_float_batch * self);
ZIGO_EXPORT void zg_float_batch_deinit(zg_float_batch * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return;
    }
zg_float_batch_deinit_impl(self);
    zg_panic_active = 0;
}

int32_t zg_pipeline_create_impl(const uint8_t * name_ptr, size_t name_len, uint32_t mode, size_t userdata, zg_pipeline * * out_result);
ZIGO_EXPORT int32_t zg_pipeline_create(const uint8_t * name_ptr, size_t name_len, uint32_t mode, size_t userdata, zg_pipeline * * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_pipeline_create_impl(name_ptr, name_len, mode, userdata, out_result);
    zg_panic_active = 0;
    return result;
}

int32_t zg_pipeline_process_impl(zg_pipeline * self, const int32_t * values_ptr, size_t values_len, int64_t * out_result);
ZIGO_EXPORT int32_t zg_pipeline_process(zg_pipeline * self, const int32_t * values_ptr, size_t values_len, int64_t * out_result) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_pipeline_process_impl(self, values_ptr, values_len, out_result);
    zg_panic_active = 0;
    return result;
}

void zg_pipeline_name_impl(zg_pipeline * self, const uint8_t * * out_result_ptr, size_t * out_result_len);
ZIGO_EXPORT void zg_pipeline_name(zg_pipeline * self, const uint8_t * * out_result_ptr, size_t * out_result_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return;
    }
zg_pipeline_name_impl(self, out_result_ptr, out_result_len);
    zg_panic_active = 0;
}

void zg_sample_values_impl(const float * * out_result_ptr, size_t * out_result_len);
ZIGO_EXPORT void zg_sample_values(const float * * out_result_ptr, size_t * out_result_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return;
    }
zg_sample_values_impl(out_result_ptr, out_result_len);
    zg_panic_active = 0;
}

int32_t zg_sample_values_checked_impl(const float * * out_result_ptr, size_t * out_result_len);
ZIGO_EXPORT int32_t zg_sample_values_checked(const float * * out_result_ptr, size_t * out_result_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_sample_values_checked_impl(out_result_ptr, out_result_len);
    zg_panic_active = 0;
    return result;
}

void zg_take_samples_impl(float * * out_result_ptr, size_t * out_result_len);
ZIGO_EXPORT void zg_take_samples(float * * out_result_ptr, size_t * out_result_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return;
    }
zg_take_samples_impl(out_result_ptr, out_result_len);
    zg_panic_active = 0;
}

int32_t zg_take_samples_checked_impl(float * * out_result_ptr, size_t * out_result_len);
ZIGO_EXPORT int32_t zg_take_samples_checked(float * * out_result_ptr, size_t * out_result_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return -2;
    }
    int32_t result = zg_take_samples_checked_impl(out_result_ptr, out_result_len);
    zg_panic_active = 0;
    return result;
}

void zg_release_samples_impl(float * samples_ptr, size_t samples_len);
ZIGO_EXPORT void zg_release_samples(float * samples_ptr, size_t samples_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return;
    }
zg_release_samples_impl(samples_ptr, samples_len);
    zg_panic_active = 0;
}

uint32_t zg_pipeline_mode_impl(zg_pipeline * self);
ZIGO_EXPORT uint32_t zg_pipeline_mode(zg_pipeline * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    uint32_t result = zg_pipeline_mode_impl(self);
    zg_panic_active = 0;
    return result;
}

uint8_t zg_pipeline_set_enabled_impl(zg_pipeline * self, uint8_t enabled);
ZIGO_EXPORT uint8_t zg_pipeline_set_enabled(zg_pipeline * self, uint8_t enabled) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    uint8_t result = zg_pipeline_set_enabled_impl(self, enabled);
    zg_panic_active = 0;
    return result;
}

size_t zg_pipeline_processed_impl(zg_pipeline * self);
ZIGO_EXPORT size_t zg_pipeline_processed(zg_pipeline * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    size_t result = zg_pipeline_processed_impl(self);
    zg_panic_active = 0;
    return result;
}

int64_t zg_pipeline_total_impl(zg_pipeline * self);
ZIGO_EXPORT int64_t zg_pipeline_total(zg_pipeline * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    int64_t result = zg_pipeline_total_impl(self);
    zg_panic_active = 0;
    return result;
}

void zg_pipeline_deinit_impl(zg_pipeline * self);
ZIGO_EXPORT void zg_pipeline_deinit(zg_pipeline * self) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return;
    }
zg_pipeline_deinit_impl(self);
    zg_panic_active = 0;
}

size_t zg_live_bytes_impl(void);
ZIGO_EXPORT size_t zg_live_bytes(void) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    size_t result = zg_live_bytes_impl();
    zg_panic_active = 0;
    return result;
}

size_t zg_compression_bound_impl(size_t source_len);
ZIGO_EXPORT size_t zg_compression_bound(size_t source_len) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    size_t result = zg_compression_bound_impl(source_len);
    zg_panic_active = 0;
    return result;
}

const char * zg_echo_c_string_impl(const char * text);
ZIGO_EXPORT const char * zg_echo_c_string(const char * text) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    const char * result = zg_echo_c_string_impl(text);
    zg_panic_active = 0;
    return result;
}

size_t zg_extract_paths_impl(const uint8_t * paths_data, size_t paths_data_len, const size_t * paths_lens, size_t paths_count, const uint8_t * sentinelSlices_data, size_t sentinelSlices_data_len, const size_t * sentinelSlices_lens, size_t sentinelSlices_count, const uint8_t * sentinelPointers_data, size_t sentinelPointers_data_len, const size_t * sentinelPointers_lens, size_t sentinelPointers_count);
ZIGO_EXPORT size_t zg_extract_paths(const uint8_t * paths_data, size_t paths_data_len, const size_t * paths_lens, size_t paths_count, const uint8_t * sentinelSlices_data, size_t sentinelSlices_data_len, const size_t * sentinelSlices_lens, size_t sentinelSlices_count, const uint8_t * sentinelPointers_data, size_t sentinelPointers_data_len, const size_t * sentinelPointers_lens, size_t sentinelPointers_count) {
    zg_panic_active = 1;
    if (setjmp(zg_panic_env) != 0) {
        zg_panic_active = 0;
        return 0;
    }
    size_t result = zg_extract_paths_impl(paths_data, paths_data_len, paths_lens, paths_count, sentinelSlices_data, sentinelSlices_data_len, sentinelSlices_lens, sentinelSlices_count, sentinelPointers_data, sentinelPointers_data_len, sentinelPointers_lens, sentinelPointers_count);
    zg_panic_active = 0;
    return result;
}
