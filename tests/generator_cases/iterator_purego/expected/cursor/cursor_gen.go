// Code generated by zigo. DO NOT EDIT.

// Package cursor provides Go bindings generated by zigo.
package cursor

import (
	"iter"

	"example.com/zigo/cursor/internal/raw"
)

// LibraryError reports a native shared-library loading failure.
type LibraryError = raw.LibraryError

// LoadLibrary atomically loads all generated native entry points.
func LoadLibrary(path string) error { return raw.LoadLibrary(path) }

// LibraryLoaded reports whether the native call surface is ready.
func LibraryLoaded() bool { return raw.LibraryLoaded() }

// DefaultLibraryName is the installed shared-library basename for the running platform.
// LoadLibrary falls back to it when no explicit path and no ZIGO_LIBRARY_PATH are set.
var DefaultLibraryName = raw.DefaultLibraryName


// Next calls the Zig function Cursor.next.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (c *Cursor) Next() (int64, bool, error) {
	ptr, err := zigoCheckedPointer("Cursor.Next receiver", c)
	if err != nil {
		return 0, false, err
	}
	defer c.zigoRelease()
	result, zigoHas, code := raw.CursorNext(ptr)
	if code != 0 {
		return 0, false, zigoPoisonAfterPanic(errorForCode("Cursor.Next", code), c)
	}
	return result, zigoHas, nil
}

// All returns a sequence that calls Next until it reports no value.
// A failed call yields its error once, with the zero int64, and the sequence ends.
func (c *Cursor) All() iter.Seq2[int64, error] {
	return func(yield func(int64, error) bool) {
		for {
			value, ok, err := c.Next()
			if err != nil {
				var zero int64
				yield(zero, err)
				return
			}
			if !ok || !yield(value, nil) {
				return
			}
		}
	}
}

// NextChecked calls the Zig function Cursor.nextChecked.
// It returns *HandleError if a required handle is nil or closed.
// Native failures are returned as generated error values.
func (c *Cursor) NextChecked() (Step, bool, error) {
	ptr, err := zigoCheckedPointer("Cursor.NextChecked receiver", c)
	if err != nil {
		return 0, false, err
	}
	defer c.zigoRelease()
	result, zigoHas, code := raw.CursorNextChecked(ptr)
	if code != 0 {
		return 0, false, zigoPoisonAfterPanic(errorForCode("Cursor.NextChecked", code), c)
	}
	return Step(result), zigoHas, nil
}

// Checked returns a sequence that calls NextChecked until it reports no value.
// A failed call yields its error once, with the zero Step, and the sequence ends.
func (c *Cursor) Checked() iter.Seq2[Step, error] {
	return func(yield func(Step, error) bool) {
		for {
			value, ok, err := c.NextChecked()
			if err != nil {
				var zero Step
				yield(zero, err)
				return
			}
			if !ok || !yield(value, nil) {
				return
			}
		}
	}
}

// Reset calls the Zig function Cursor.reset.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (c *Cursor) Reset() error {
	ptr, err := zigoCheckedPointer("Cursor.Reset receiver", c)
	if err != nil {
		return err
	}
	defer c.zigoRelease()
	code := raw.CursorReset(ptr)
	if code != 0 {
		return zigoPoisonAfterPanic(errorForCode("Cursor.Reset", code), c)
	}
	return nil
}
