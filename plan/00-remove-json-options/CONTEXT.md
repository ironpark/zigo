# SCOPE

Edit the command examples, output-selection guidance, draft instructions, and error-handling guidance in `tool.md`.

# CONTEXT

## Current implementation and bottlenecks

The document recommends `--json` for several commands and describes JSON-specific return structures.

## Target structure and invariants

Keep the planr workflow, command coverage, stdin/stdout guidance, validation behavior, and phase lifecycle intact without referring to JSON options.
