# GOALS

## Problem and the end result from the user's point of view

`tool.md` currently documents JSON-specific flags and output handling that the user wants removed.

## Measurable goals

Remove every JSON option and JSON-specific instruction from `tool.md` while keeping the remaining CLI workflow coherent.

## Supported scope and non-goals

Only `tool.md` documentation is in scope. The `planr` CLI implementation is not changed.

## Reference source / commit / license

The current repository version of `tool.md` is the sole reference.

## Completion criteria for the whole plan

No JSON option or JSON-specific guidance remains in `tool.md`, and the edited document reads coherently.
