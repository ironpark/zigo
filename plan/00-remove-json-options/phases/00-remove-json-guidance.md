---
perf_phase: false
status: in-progress
---
> DONE-WHEN: A case-insensitive search finds no JSON references in `tool.md`, and the diff contains only the intended documentation edits.
> NEXT: none

# Remove JSON guidance

## Planned Work

- Remove JSON flags and JSON-specific prose from `tool.md`, then revise affected sentences for continuity.

## Done When

- A case-insensitive search finds no JSON references in `tool.md`, and the diff contains only the intended documentation edits.
