---
perf_phase: false
status: in-progress
---
> DONE-WHEN: The regression fails before the fix and passes afterwards; zig build test passes; changes are committed.
> NEXT: none

# Initial Work

## Planned Work

- Reproduce the external plugin helper failure through addGoBindings; wire naming and add a consumer regression test plus changelog.

## Done When

- The regression fails before the fix and passes afterwards; zig build test passes; changes are committed.
