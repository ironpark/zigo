# SCOPE

Changelog, repository release checks, version/install instructions, tag push and GitHub Release verification.

# CONTEXT

Current version is 0.16.1; the new semantic extension contract warrants a minor version under repository policy. The user authorized release. Origin/main is an ancestor of the clean local main.
