---
perf_phase: false
status: planned
---
> DONE-WHEN: `80_cols` tag가 생성되고 `go vet` 통과, 커밋.
> NEXT: none

# emit 이름 기준 식별자 검사

## Planned Work

- enum tag 검사를 `<Type><Pascal(tag)>` 기준으로 바꾸고, 숫자 시작 tag 골든과 ZIGO021 스냅샷 조정.

## Done When

- `80_cols` tag가 생성되고 `go vet` 통과, 커밋.
