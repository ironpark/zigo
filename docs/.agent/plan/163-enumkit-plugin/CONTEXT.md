# SCOPE

Enum helpers only; no automatic native calls or ABI changes.

# CONTEXT

Existing type hooks and liveFields supply the generated enum constants.

## Current implementation and bottlenecks

Add a Go-only enumkit package providing ordered fresh value slices and known-tag membership, preserving ABI.

## Target structure and invariants

Add a Go-only enumkit package providing ordered fresh value slices and known-tag membership, preserving ABI.
