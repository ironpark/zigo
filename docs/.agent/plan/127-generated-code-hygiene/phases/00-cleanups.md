---
perf_phase: false
status: in-progress
---
> DONE-WHEN: `zig build test` and every example's `go test` pass on both backends.
> NEXT: none

# Template cleanups

## Planned Work

- Apply the four template changes, refresh goldens, regenerate examples, run tests, add a CHANGELOG line.

## Done When

- `zig build test` and every example's `go test` pass on both backends.
