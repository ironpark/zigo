---
perf_phase: false
status: in-progress
---
> DONE-WHEN: Focused public-module tests and `zig build test --summary all` pass; documentation demonstrates a mixed collected list with no wildcard final paths.
> NEXT: none

# Initial Work

## Planned Work

- Add concise ownership methods, selector defaults/exclusions, and `dsl.collect`.
- Add unit tests and update function DSL documentation and changelog.
- Run formatting and the full test suite.

## Done When

- Focused public-module tests and `zig build test --summary all` pass; documentation demonstrates a mixed collected list with no wildcard final paths.
