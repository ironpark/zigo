---
perf_phase: false
status: planned
---
> DONE-WHEN: A wrapper with `.covers` moves its target from `unbound` to `wrapped` and the percentage counts it; verification loop green.
> NEXT: none

# Wrapper coverage via `.covers`

## Planned Work

- Schema, reflection validation, semantic.json field, abi-diff ignore, report `wrapped` section and JSON, tests, example, docs, CHANGELOG.

## Done When

- A wrapper with `.covers` moves its target from `unbound` to `wrapped` and the percentage counts it; verification loop green.
