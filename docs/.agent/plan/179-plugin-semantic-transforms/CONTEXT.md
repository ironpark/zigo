# SCOPE

A typed transform context and ordered pipeline, adapter selection for existing supported conversion sites, exact public function naming, safe native argument ordering, external integration tests and author documentation.

# CONTEXT

The current contract validates and analyzes read-only IR. Existing GoAdapter lowering already implements conversions. Reuse it rather than intercepting rendered type text. Run all transformations before final core and plugin validation, and share the transformed document through lowering and analysis.
