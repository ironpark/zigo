# GOALS

Plugins can rewrite the semantic document before validation and lowering, select Go adapters, and prescribe exact public function and registered type names. Transformations explicitly own ABI changes; rendering stays additive.
