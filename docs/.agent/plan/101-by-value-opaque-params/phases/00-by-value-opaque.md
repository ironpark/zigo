---
perf_phase: false
status: planned
---
> DONE-WHEN: `fn f(self: Screen)` and `fn g(screen: Screen)` bind without wrappers and pass Go tests on both backends.
> NEXT: none

# By-value opaque receivers and parameters

## Planned Work

- Reflection marker, validation, shim deref emission on cgo and purego, tests, generator case, example, docs, CHANGELOG.

## Done When

- `fn f(self: Screen)` and `fn g(screen: Screen)` bind without wrappers and pass Go tests on both backends.
