---
completed_at: "2026-09-06T06:23:54Z"
perf_phase: false
status: done
---
> DONE-WHEN: Coverage unit test shows an enum method as `wrapped <- Enum`.
> NEXT: none

# `.covers` on enum type entries

## Planned Work

- Validate `.covers` on `.enumeration` entries at comptime and apply them in
  `coverage.zig` with the enum name as `via`.

## Done When

- Coverage unit test shows an enum method as `wrapped <- Enum`.
