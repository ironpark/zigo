---
perf_phase: false
status: planned
---
> DONE-WHEN: Examples with custom layouts build, link, and pass Go tests on cgo and purego from the configured directories; all other examples' generated files are unchanged; verification loop green.
> NEXT: none

# Configurable install locations

## Planned Work

- Add the `install` option, route all path derivation through it, keep defaults byte-identical, add the same-header-name panic, update purego loader defaults, opt two examples into custom layouts with tests, docs, CHANGELOG.

## Done When

- Examples with custom layouts build, link, and pass Go tests on cgo and purego from the configured directories; all other examples' generated files are unchanged; verification loop green.
