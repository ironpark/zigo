# GOALS

Plugins can emit framed Go source, tests and examples into the public, external test or raw package, and arbitrary byte artifacts without Go framing. No legacy File compatibility layer remains.
