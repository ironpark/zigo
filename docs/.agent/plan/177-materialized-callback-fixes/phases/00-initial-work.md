---
perf_phase: false
status: in-progress
---
> DONE-WHEN: Tests verify exact release targets, invalid release diagnostics, and callback registration lifecycle on both backends; full tests pass and changes are committed.
> NEXT: none

# Initial Work

## Planned Work

- Preserve materialized release identity across package filtering; reject string release semantics; check nil callbacks before handle allocation; add regression coverage and regenerate affected outputs.

## Done When

- Tests verify exact release targets, invalid release diagnostics, and callback registration lifecycle on both backends; full tests pass and changes are committed.
