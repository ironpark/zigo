---
perf_phase: false
status: in-progress
---
> DONE-WHEN: With the option on, every eligible function has a `Must*` twin proven by a Go test on both backends; with it off, no output changes.
> NEXT: none

# Must variants

## Planned Work

- Option plumbing, renderer, collision check, generator case, example, docs, CHANGELOG.

## Done When

- With the option on, every eligible function has a `Must*` twin proven by a Go test on both backends; with it off, no output changes.
