---
perf_phase: false
status: in-progress
---
> DONE-WHEN: Goldens and example tests pass with the accessors.
> NEXT: none

# Value union accessors

## Planned Work

- Emit `As<Variant>() (payload, bool)` for payload variants of value-only tagged unions; update goldens, example, docs.

## Done When

- Goldens and example tests pass with the accessors.
