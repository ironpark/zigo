// Code generated by zigo. DO NOT EDIT.

// Package raw is the generated call layer between the public package and
// the native library. It is not part of the supported API.
package raw

/*
#cgo CFLAGS: -I${SRCDIR}/../../../zig-out/include
#cgo LDFLAGS: ${SRCDIR}/../../../zig-out/lib/libstreams_zigo.a
#include "zigo_streams.h"
*/
import "C"
import "io"
import "runtime/cgo"
import "runtime/debug"
import "sync"
import "unsafe"

// LastErrorMessage returns the most recent native panic message for this binding.
func LastErrorMessage() string { return C.GoString(C.zg_last_error_message()) }

// CallbackState carries one Go callback across the native boundary, and
// the panic it raises there until the generated caller rethrows it. The
// trampoline has to recover: a panic cannot unwind native frames.
type CallbackState struct {
	Fn       any
	Writer   io.Writer
	Reader   io.Reader
	mu       sync.Mutex
	value    any
	stack    []byte
	panicked bool
	err      error
}

func (state *CallbackState) record(value any) {
	state.mu.Lock()
	defer state.mu.Unlock()
	if state.panicked {
		return
	}
	state.panicked = true
	state.value = value
	state.stack = debug.Stack()
}

// TakeCallbackPanic returns and clears the panic the callback behind handle recorded.
func TakeCallbackPanic(handle cgo.Handle) (any, []byte, bool) {
	state := handle.Value().(*CallbackState)
	state.mu.Lock()
	defer state.mu.Unlock()
	if !state.panicked {
		return nil, nil, false
	}
	value, stack := state.value, state.stack
	state.value, state.stack, state.panicked = nil, nil, false
	return value, stack, true
}

// recordErr keeps the first error a Go stream reported, until the generated
// caller takes it. Later crossings are not asked: once a stream has failed
// the adapter stops using it.
func (state *CallbackState) recordErr(err error) {
	state.mu.Lock()
	defer state.mu.Unlock()
	if state.err == nil {
		state.err = err
	}
}

// TakeStreamError returns and clears the error the Go stream behind handle reported.
func TakeStreamError(handle cgo.Handle) (error, bool) {
	state := handle.Value().(*CallbackState)
	state.mu.Lock()
	defer state.mu.Unlock()
	if state.err == nil {
		return nil, false
	}
	err := state.err
	state.err = nil
	return err, true
}

//export zg_zigo_stream_write
func zg_zigo_stream_write(p0 *C.uint8_t, p1 C.size_t, p2 C.size_t) (result C.int32_t) {
	state := cgo.Handle(p2).Value().(*CallbackState)
	defer func() {
		if value := recover(); value != nil {
			state.record(value)
			result = C.int32_t(-3)
		}
	}()
	n, err := state.Writer.Write(unsafe.Slice((*byte)(unsafe.Pointer(p0)), int(p1)))
	if err == nil && n != int(p1) {
		err = io.ErrShortWrite
	}
	if err != nil {
		state.recordErr(err)
		return C.int32_t(-1)
	}
	return C.int32_t(0)
}

//export zg_zigo_stream_read
func zg_zigo_stream_read(p0 *C.uint8_t, p1 C.size_t, p2 C.size_t) (result C.int32_t) {
	state := cgo.Handle(p2).Value().(*CallbackState)
	defer func() {
		if value := recover(); value != nil {
			state.record(value)
			result = C.int32_t(-3)
		}
	}()
	n, err := state.Reader.Read(unsafe.Slice((*byte)(unsafe.Pointer(p0)), int(p1)))
	if n > 0 {
		return C.int32_t(n)
	}
	if err == nil || err == io.EOF {
		return C.int32_t(0)
	}
	state.recordErr(err)
	return C.int32_t(-1)
}

// DocumentCreate calls the generated C ABI wrapper for zg_document_create.
func DocumentCreate() (unsafe.Pointer, int32) {
	var outResult *C.zg_document
	code := int32(C.zg_document_create(&outResult))
	return unsafe.Pointer(outResult), code
}

// DocumentDeinit calls the generated C ABI wrapper for zg_document_deinit.
func DocumentDeinit(self unsafe.Pointer) int32 {
	code := int32(C.zg_document_deinit((*C.zg_document)(self)))
	return code
}

// DocumentAppend calls the generated C ABI wrapper for zg_document_append.
func DocumentAppend(self unsafe.Pointer, line []uint8) int32 {
	var lineZero C.uint8_t
	linePtr := &lineZero
	if len(line) != 0 {
		linePtr = (*C.uint8_t)(unsafe.Pointer(&line[0]))
	}
	code := int32(C.zg_document_append((*C.zg_document)(self), linePtr, C.size_t(len(line))))
	return code
}

// DocumentCount calls the generated C ABI wrapper for zg_document_count.
func DocumentCount(self unsafe.Pointer) (uint, int32) {
	var outResult C.size_t
	code := int32(C.zg_document_count((*C.zg_document)(self), &outResult))
	return uint(outResult), code
}

// DocumentDump calls the generated C ABI wrapper for zg_document_dump.
func DocumentDump(self unsafe.Pointer, wHandle uintptr) int32 {
	code := int32(C.zg_document_dump((*C.zg_document)(self), C.size_t(wHandle)))
	return code
}

// DocumentLoad calls the generated C ABI wrapper for zg_document_load.
func DocumentLoad(self unsafe.Pointer, rHandle uintptr) (uint, int32) {
	var outResult C.size_t
	code := int32(C.zg_document_load((*C.zg_document)(self), nil, C.size_t(0), C.size_t(rHandle), &outResult))
	return uint(outResult), code
}

// Banner calls the generated C ABI wrapper for zg_banner.
func Banner(wHandle uintptr, width uint32) int32 {
	code := int32(C.zg_banner(C.size_t(wHandle), C.uint32_t(width)))
	return code
}

// Tee calls the generated C ABI wrapper for zg_tee.
func Tee(rHandle uintptr, wHandle uintptr) (uint, int32) {
	var outResult C.size_t
	code := int32(C.zg_tee(nil, C.size_t(0), C.size_t(rHandle), C.size_t(wHandle), &outResult))
	return uint(outResult), code
}
