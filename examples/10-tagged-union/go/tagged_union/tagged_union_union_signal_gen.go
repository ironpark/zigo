// Code generated by zigo. DO NOT EDIT.

package tagged_union

import (
	"runtime"

	"example.com/zigo/tagged-union/internal/raw"
)

func zigoSignalTag(receiver zigoHandle) (SignalTag, error) {
	defer runtime.KeepAlive(receiver)
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Signal.Tag receiver", receiver)
	if err != nil {
		return 0, err
	}
	defer receiver.zigoRelease()
	result, status := raw.SignalProjectTag(ptr)
	if status != zigoProjectionSuccess {
		return 0, zigoPoisonAfterPanic(zigoProjectionError("Signal.Tag", status), receiver)
	}
	return SignalTag(result), nil
}

// Tag returns the active tagged-union tag or a typed lifecycle/native error.
func (s *Signal) Tag() (SignalTag, error) { return zigoSignalTag(s) }

// MustTag returns the active tagged-union tag and panics with a typed error on failure.
func (s *Signal) MustTag() SignalTag { return zigoMust(zigoSignalTag(s)) }

func zigoSignalAsTicks(receiver zigoHandle) (uint32, bool, error) {
	defer runtime.KeepAlive(receiver)
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Signal.AsTicks receiver", receiver)
	if err != nil {
		return 0, false, err
	}
	defer receiver.zigoRelease()
	result, status := raw.SignalProjectTicks(ptr)
	if status == zigoProjectionMismatch {
		return 0, false, nil
	}
	if status != zigoProjectionSuccess {
		return 0, false, zigoPoisonAfterPanic(zigoProjectionError("Signal.AsTicks", status), receiver)
	}
	return result, true, nil
}

// AsTicks returns the ticks payload, whether it is active, and any lifecycle/native error.
func (s *Signal) AsTicks() (uint32, bool, error) { return zigoSignalAsTicks(s) }

// MustAsTicks returns the ticks payload when active and panics with a typed error on failure.
func (s *Signal) MustAsTicks() (uint32, bool) { return zigoMustMatch(zigoSignalAsTicks(s)) }

func zigoSignalAsLevel(receiver zigoHandle) (float64, bool, error) {
	defer runtime.KeepAlive(receiver)
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Signal.AsLevel receiver", receiver)
	if err != nil {
		return 0, false, err
	}
	defer receiver.zigoRelease()
	result, status := raw.SignalProjectLevel(ptr)
	if status == zigoProjectionMismatch {
		return 0, false, nil
	}
	if status != zigoProjectionSuccess {
		return 0, false, zigoPoisonAfterPanic(zigoProjectionError("Signal.AsLevel", status), receiver)
	}
	return result, true, nil
}

// AsLevel returns the level payload, whether it is active, and any lifecycle/native error.
func (s *Signal) AsLevel() (float64, bool, error) { return zigoSignalAsLevel(s) }

// MustAsLevel returns the level payload when active and panics with a typed error on failure.
func (s *Signal) MustAsLevel() (float64, bool) { return zigoMustMatch(zigoSignalAsLevel(s)) }

func zigoSignalAsOffset(receiver zigoHandle) (int16, bool, error) {
	defer runtime.KeepAlive(receiver)
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Signal.AsOffset receiver", receiver)
	if err != nil {
		return 0, false, err
	}
	defer receiver.zigoRelease()
	result, status := raw.SignalProjectOffset(ptr)
	if status == zigoProjectionMismatch {
		return 0, false, nil
	}
	if status != zigoProjectionSuccess {
		return 0, false, zigoPoisonAfterPanic(zigoProjectionError("Signal.AsOffset", status), receiver)
	}
	return result, true, nil
}

// AsOffset returns the offset payload, whether it is active, and any lifecycle/native error.
func (s *Signal) AsOffset() (int16, bool, error) { return zigoSignalAsOffset(s) }

// MustAsOffset returns the offset payload when active and panics with a typed error on failure.
func (s *Signal) MustAsOffset() (int16, bool) { return zigoMustMatch(zigoSignalAsOffset(s)) }

func zigoSignalAsMode(receiver zigoHandle) (Mode, bool, error) {
	defer runtime.KeepAlive(receiver)
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Signal.AsMode receiver", receiver)
	if err != nil {
		return 0, false, err
	}
	defer receiver.zigoRelease()
	result, status := raw.SignalProjectMode(ptr)
	if status == zigoProjectionMismatch {
		return 0, false, nil
	}
	if status != zigoProjectionSuccess {
		return 0, false, zigoPoisonAfterPanic(zigoProjectionError("Signal.AsMode", status), receiver)
	}
	return Mode(result), true, nil
}

// AsMode returns the mode payload, whether it is active, and any lifecycle/native error.
func (s *Signal) AsMode() (Mode, bool, error) { return zigoSignalAsMode(s) }

// MustAsMode returns the mode payload when active and panics with a typed error on failure.
func (s *Signal) MustAsMode() (Mode, bool) { return zigoMustMatch(zigoSignalAsMode(s)) }

func zigoSignalAsActive(receiver zigoHandle) (bool, bool, error) {
	defer runtime.KeepAlive(receiver)
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Signal.AsActive receiver", receiver)
	if err != nil {
		return false, false, err
	}
	defer receiver.zigoRelease()
	result, status := raw.SignalProjectActive(ptr)
	if status == zigoProjectionMismatch {
		return false, false, nil
	}
	if status != zigoProjectionSuccess {
		return false, false, zigoPoisonAfterPanic(zigoProjectionError("Signal.AsActive", status), receiver)
	}
	return result != 0, true, nil
}

// AsActive returns the active payload, whether it is active, and any lifecycle/native error.
func (s *Signal) AsActive() (bool, bool, error) { return zigoSignalAsActive(s) }

// MustAsActive returns the active payload when active and panics with a typed error on failure.
func (s *Signal) MustAsActive() (bool, bool) { return zigoMustMatch(zigoSignalAsActive(s)) }

// SignalSnapshot is a value copy of a Signal: one native call carries the active tag
// and every scalar payload back together.
type SignalSnapshot struct {
	tag    SignalTag
	level  float64
	ticks  uint32
	offset int16
	mode   Mode
	active bool
}

// Tag returns the variant the snapshot captured.
func (snapshot SignalSnapshot) Tag() SignalTag {
	return snapshot.tag
}

// Level returns the level payload and whether it is the captured variant.
func (snapshot SignalSnapshot) Level() (float64, bool) {
	return snapshot.level, snapshot.tag == SignalTagLevel
}

// Ticks returns the ticks payload and whether it is the captured variant.
func (snapshot SignalSnapshot) Ticks() (uint32, bool) {
	return snapshot.ticks, snapshot.tag == SignalTagTicks
}

// Offset returns the offset payload and whether it is the captured variant.
func (snapshot SignalSnapshot) Offset() (int16, bool) {
	return snapshot.offset, snapshot.tag == SignalTagOffset
}

// Mode returns the mode payload and whether it is the captured variant.
func (snapshot SignalSnapshot) Mode() (Mode, bool) {
	return snapshot.mode, snapshot.tag == SignalTagMode
}

// Active returns the active payload and whether it is the captured variant.
func (snapshot SignalSnapshot) Active() (bool, bool) {
	return snapshot.active, snapshot.tag == SignalTagActive
}

func zigoSignalSnapshot(receiver zigoHandle) (SignalSnapshot, error) {
	defer runtime.KeepAlive(receiver)
	runtime.LockOSThread()
	defer runtime.UnlockOSThread()
	ptr, err := zigoCheckedPointer("Signal.Snapshot receiver", receiver)
	if err != nil {
		return SignalSnapshot{}, err
	}
	defer receiver.zigoRelease()
	data, status := raw.SignalReadSnapshot(ptr)
	if status != zigoProjectionSuccess {
		return SignalSnapshot{}, zigoPoisonAfterPanic(zigoProjectionError("Signal.Snapshot", status), receiver)
	}
	return SignalSnapshot{
		tag:    SignalTag(data.Tag),
		level:  data.Level,
		ticks:  data.Ticks,
		offset: data.Offset,
		mode:   Mode(data.Mode),
		active: data.Active != 0,
	}, nil
}

// Snapshot reads the tag and every payload in one native call, or
// returns a typed lifecycle/native error.
func (s *Signal) Snapshot() (SignalSnapshot, error) { return zigoSignalSnapshot(s) }

// MustSnapshot reads the tag and every payload in one native call and panics
// with a typed error on failure.
func (s *Signal) MustSnapshot() SignalSnapshot { return zigoMust(zigoSignalSnapshot(s)) }

// SignalVariant is the sealed interface every Signal variant implements. A type
// switch over the concrete variant types reads the active payload without
// probing each projection in turn.
type SignalVariant interface{ isSignalVariant() }

// SignalIdle is the idle variant of Signal. It carries no payload.
type SignalIdle struct{}

func (SignalIdle) isSignalVariant() {}

// SignalTicks is the ticks variant of Signal.
type SignalTicks struct {
	// Value is the payload the ticks variant carries.
	Value uint32
}

func (SignalTicks) isSignalVariant() {}

// SignalLevel is the level variant of Signal.
type SignalLevel struct {
	// Value is the payload the level variant carries.
	Value float64
}

func (SignalLevel) isSignalVariant() {}

// SignalOffset is the offset variant of Signal.
type SignalOffset struct {
	// Value is the payload the offset variant carries.
	Value int16
}

func (SignalOffset) isSignalVariant() {}

// SignalMode is the mode variant of Signal.
type SignalMode struct {
	// Value is the payload the mode variant carries.
	Value Mode
}

func (SignalMode) isSignalVariant() {}

// SignalActive is the active variant of Signal.
type SignalActive struct {
	// Value is the payload the active variant carries.
	Value bool
}

func (SignalActive) isSignalVariant() {}

// The snapshot reader takes the receiver's read lock for its single
// native call, so tag and payload are always read together.
func zigoSignalVariant(receiver zigoHandle) (SignalVariant, error) {
	data, err := zigoSignalSnapshot(receiver)
	if err != nil {
		return nil, err
	}
	switch data.tag {
	case SignalTagIdle:
		return SignalIdle{}, nil
	case SignalTagTicks:
		return SignalTicks{Value: data.ticks}, nil
	case SignalTagLevel:
		return SignalLevel{Value: data.level}, nil
	case SignalTagOffset:
		return SignalOffset{Value: data.offset}, nil
	case SignalTagMode:
		return SignalMode{Value: data.mode}, nil
	case SignalTagActive:
		return SignalActive{Value: data.active}, nil
	default:
		return nil, zigoProjectionError("Signal.Variant", zigoProjectionMismatch)
	}
}

// Variant returns the active variant as a concrete SignalVariant, or a typed
// lifecycle/native error.
func (s *Signal) Variant() (SignalVariant, error) { return zigoSignalVariant(s) }

// MustVariant returns the active variant as a concrete SignalVariant and panics
// with a typed error on failure.
func (s *Signal) MustVariant() SignalVariant { return zigoMust(zigoSignalVariant(s)) }
