// Code generated by zigo. DO NOT EDIT.

// Package materialized provides Go bindings generated by zigo.
package materialized

import (
	"iter"

	"example.com/zigo/materialized/internal/raw"
)

// Value calls the Zig function LegacyLeaf.value.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (l *LegacyLeaf) Value() (int32, error) {
	ptr, err := zigoCheckedPointer("LegacyLeaf.Value receiver", l)
	if err != nil {
		return 0, err
	}
	defer l.zigoRelease()
	result, code := raw.LegacyLeafValue(ptr)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(zigoErrorForCode("LegacyLeaf.Value", code), l)
	}
	return result, nil
}

// NewLegacyProbe creates a caller-owned LegacyProbe.
// The caller must call Close on the returned handle.
// Native failures are returned as generated error values.
func NewLegacyProbe(index uint) (*LegacyProbe, error) {
	result, code := raw.LegacyProbeCreate(index)
	if code != 0 {
		return nil, zigoErrorForCode("NewLegacyProbe", code)
	}
	return zigoNewLegacyProbe(result), nil
}

// ID calls the Zig function LegacyProbe.id.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (l *LegacyProbe) ID() (uint64, error) {
	ptr, err := zigoCheckedPointer("LegacyProbe.ID receiver", l)
	if err != nil {
		return 0, err
	}
	defer l.zigoRelease()
	result, code := raw.LegacyProbeID(ptr)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(zigoErrorForCode("LegacyProbe.ID", code), l)
	}
	return result, nil
}

// Active calls the Zig function LegacyProbe.active.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (l *LegacyProbe) Active() (bool, error) {
	ptr, err := zigoCheckedPointer("LegacyProbe.Active receiver", l)
	if err != nil {
		return false, err
	}
	defer l.zigoRelease()
	result, code := raw.LegacyProbeActive(ptr)
	if code != 0 {
		return false, zigoPoisonAfterPanic(zigoErrorForCode("LegacyProbe.Active", code), l)
	}
	return result != 0, nil
}

// Child calls the Zig function LegacyProbe.child.
// The returned reference remains valid only while its parent handle remains open.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (l *LegacyProbe) Child() (*LegacyLeaf, error) {
	ptr, err := zigoCheckedPointer("LegacyProbe.Child receiver", l)
	if err != nil {
		return nil, err
	}
	defer l.zigoRelease()
	result, code := raw.LegacyProbeChild(ptr)
	if code != 0 {
		return nil, zigoPoisonAfterPanic(zigoErrorForCode("LegacyProbe.Child", code), l)
	}
	return zigoNewBorrowedLegacyLeaf(result, l), nil
}

// NewCursor creates a caller-owned Cursor.
// The caller must call Close on the returned handle.
// Native failures are returned as generated error values.
func NewCursor(limit uint32, failAt uint32) (*Cursor, error) {
	result, code := raw.CursorCreate(limit, failAt)
	if code != 0 {
		return nil, zigoErrorForCode("NewCursor", code)
	}
	return zigoNewCursor(result), nil
}

// Next calls the Zig function Cursor.next.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (c *Cursor) Next() (Probe, bool, error) {
	ptr, err := zigoCheckedPointer("Cursor.Next receiver", c)
	if err != nil {
		return Probe{}, false, err
	}
	defer c.zigoRelease()
	result, zigoHas, code := raw.CursorNext(ptr)
	if code != 0 {
		return Probe{}, false, zigoPoisonAfterPanic(zigoErrorForCode("Cursor.Next", code), c)
	}
	if !zigoHas {
		return Probe{}, false, nil
	}
	defer raw.Release(result)
	return zigoDecodeProbeBuffer(result), zigoHas, nil
}

// All returns a sequence that calls Next until it reports no value.
// A failed call yields its error once, with the zero Probe, and the sequence ends.
func (c *Cursor) All() iter.Seq2[Probe, error] {
	return func(yield func(Probe, error) bool) {
		for {
			value, ok, err := c.Next()
			if err != nil {
				var zero Probe
				yield(zero, err)
				return
			}
			if !ok || !yield(value, nil) {
				return
			}
		}
	}
}

// NextChecked calls the Zig function Cursor.nextChecked.
// It returns *HandleError if a required handle is nil or closed.
// Native failures are returned as generated error values.
func (c *Cursor) NextChecked() (Probe, bool, error) {
	ptr, err := zigoCheckedPointer("Cursor.NextChecked receiver", c)
	if err != nil {
		return Probe{}, false, err
	}
	defer c.zigoRelease()
	result, zigoHas, code := raw.CursorNextChecked(ptr)
	if code != 0 {
		return Probe{}, false, zigoPoisonAfterPanic(zigoErrorForCode("Cursor.NextChecked", code), c)
	}
	if !zigoHas {
		return Probe{}, false, nil
	}
	defer raw.Release(result)
	return zigoDecodeProbeBuffer(result), zigoHas, nil
}

// Checked returns a sequence that calls NextChecked until it reports no value.
// A failed call yields its error once, with the zero Probe, and the sequence ends.
func (c *Cursor) Checked() iter.Seq2[Probe, error] {
	return func(yield func(Probe, error) bool) {
		for {
			value, ok, err := c.NextChecked()
			if err != nil {
				var zero Probe
				yield(zero, err)
				return
			}
			if !ok || !yield(value, nil) {
				return
			}
		}
	}
}

// Count calls the Zig function Cursor.count.
// It returns *HandleError if a required handle is nil or closed.
// A native panic is returned as *NativePanicError.
func (c *Cursor) Count() (uint32, error) {
	ptr, err := zigoCheckedPointer("Cursor.Count receiver", c)
	if err != nil {
		return 0, err
	}
	defer c.zigoRelease()
	result, code := raw.CursorCount(ptr)
	if code != 0 {
		return 0, zigoPoisonAfterPanic(zigoErrorForCode("Cursor.Count", code), c)
	}
	return result, nil
}

// OptionalSnapshot calls the Zig function optionalSnapshot.
func OptionalSnapshot(present bool) (Probe, bool) {
	zigoResult, zigoHas := raw.OptionalSnapshot(zigoBoolToUint8(present))
	if !zigoHas {
		return Probe{}, false
	}
	defer raw.Release(zigoResult)
	return zigoDecodeProbeBuffer(zigoResult), zigoHas
}

// OptionalBatch calls the Zig function optionalBatch.
func OptionalBatch(present bool, empty bool) ([]Probe, bool) {
	zigoResult, zigoHas := raw.OptionalBatch(zigoBoolToUint8(present), zigoBoolToUint8(empty))
	if !zigoHas {
		return nil, false
	}
	defer raw.Release(zigoResult)
	return zigoDecodeProbeSliceBuffer(zigoResult), zigoHas
}

// OptionalBatchChecked calls the Zig function optionalBatchChecked.
// Native failures are returned as generated error values.
func OptionalBatchChecked(present bool, empty bool, fail bool) ([]Probe, bool, error) {
	result, zigoHas, code := raw.OptionalBatchChecked(zigoBoolToUint8(present), zigoBoolToUint8(empty), zigoBoolToUint8(fail))
	if code != 0 {
		return nil, false, zigoErrorForCode("OptionalBatchChecked", code)
	}
	if !zigoHas {
		return nil, false, nil
	}
	defer raw.Release(result)
	return zigoDecodeProbeSliceBuffer(result), zigoHas, nil
}

// ReleasedBuffers calls the Zig function releasedBuffers.
func ReleasedBuffers() uint32 {
	return raw.ReleasedBuffers()
}

// Snapshot calls the Zig function snapshot.
func Snapshot() Probe {
	result := raw.Snapshot()
	defer raw.Release(result)
	return zigoDecodeProbeBuffer(result)
}

// ProbeMany calls the Zig function probeMany.
// Native failures are returned as generated error values.
func ProbeMany() ([]Probe, error) {
	result, code := raw.ProbeMany()
	if code != 0 {
		return nil, zigoErrorForCode("ProbeMany", code)
	}
	defer raw.Release(result)
	return zigoDecodeProbeSliceBuffer(result), nil
}

// Fill calls the Zig function fill.
func Fill(output []Probe) uint {
	zigoBuffer, result := raw.Fill(len(output))
	defer raw.Release(zigoBuffer)
	zigoDecodeProbeSliceInto(zigoBuffer, output)
	return result
}
